# Task 305 - RAH v0.25.0 Release Preparation

## Status and terminal boundary

RAH v0.25.0 is **PREPARED, NOT PUBLISHED**. v0.24.0 remains the current
immutable published release. This task does not create a tag, GitHub Release,
artifact publication, crate publication, release marker, or post-release
cleanup, and does not begin v0.26 work.

## Authoritative starting checkpoint

- Workspace: `F:\coding\otherPrj\rust-agent-harness`.
- Branch: `master`.
- Starting `HEAD`, `master`, and `origin/master`:
  `b3532f52b79be9575f3f9d8e818efa2096da4e12`.
- Direct parent: `b2e170cc6f8226891f572dce6ce453abba836aaa`.
- Task 304: **Verdict A - READY FOR RELEASE PREPARATION**.
- Task 304 exact-head CI: `34659690228` - PASS.
- Current published release: `v0.24.0`.
- No `v0.25.0` tag or GitHub Release exists.

## Authorized scope

The exact changed-file scope is:

```text
Cargo.toml
Cargo.lock
CHANGELOG.md
README.md
docs/ARCHITECTURE.md
docs/SECURITY.md
docs/RAH_V0.25_RELEASE_GATE.md
docs/plans/2026-09-12-v0.25-release-preparation.md
```

Only release-facing documentation and workspace version metadata are changed.
The workspace version moves from `0.24.0` to `0.25.0` across all 13 packages;
Rust edition remains 2024. No dependency versions drift.

## Release theme and authority architecture

The theme is **HostExplicit Reviewed File Rename/Move (`repo.rename-file`)**.
ADR 0018 remains ordinary rename/move authority. ADR 0021 remains the generic
HostExplicit coordinator/currentness/ticket/D2 boundary. ADR 0026 remains the
capability-specific reviewed boundary.

The host-owned route is typed human `{source_path, destination_path}`,
zero-effect Prepare, complete bounded non-truncated review, opaque single-use
ticket, ticket-only Confirm, currentness/exact ToolDefinition/permission/D2,
reviewed Commit authorization invalidation before Started, exactly one
authorized dispatch through the current ToolRegistry, the existing ordinary
`repo.rename-file`, independent post-effect proof, and status-only activity.

The exact 11 eligible production Tools are `fs.read`, `repo.file-info`,
`repo.status`, `repo.diff`, `repo.diff-staged`, `repo.create-branch`,
`repo.patch`, `repo.edit-files`, `repo.create-file`, `repo.delete-file`, and
`repo.rename-file`. `repo.create-directory`, `repo.commit`, MCP Tools, Process
Plugin Tools, fixture/diagnostic Tools, and unknown/provider-defined Tools are
ineligible.

The ordinary public input remains exactly `source_path`, `destination_path`,
`expected_source_file_sha256`, and `expected_source_file_byte_length`; its
permission is `PermissionLevel::Execute`; statuses are `renamed_verified`,
`known_no_effect`, `invalid_input`, `precondition_failed`, and `uncertain`; and
the native effect is at most one no-replace rename attempt. There is no retry,
replay, reverse rename, rollback, compensation, copy-delete, `git mv`, or
shell path.

The reviewed subset requires one existing clean HEAD-tracked regular source
file in the same repository, source <= 65,536 raw bytes, strict UTF-8, NUL-free
content, supported mode, link count 1, exact HEAD/index/worktree equality, an
existing destination parent, and destination absence from the worktree, HEAD,
and every index stage. Ignored destinations, Windows aliases/case-equivalent
collisions, unsupported cross-repository/cross-volume movement, overwrite,
and parent creation fail closed. The review is complete and non-truncated.
Its effect is one unstaged worktree rename/move only, with no content rewrite,
Stage, Unstage, Commit, branch/ref/history mutation, directory creation,
import/reference rewriting, or automatic retry/replay/rollback/compensation.

## Task 303 correction and carried evidence

The final v0.25 security foundation includes rejection of filesystem-equivalent
tracked/index destination paths on Windows even when spelling differs. The
implementation uses bounded HEAD tree discovery, bounded case-insensitive
index discovery, strict Git candidate parsing, and host-owned existing
filesystem path-equivalence comparison. Tracked-but-missing HEAD, index-only,
intent-to-add, and conflict/unmerged collisions reject before native effect
with zero native attempts.

Task 303 corrected production commit:
`1aa4d3277bd7ba24dec1caa3d52cc57c2aa2584a`.
Later Task 303 validation-documentation commit:
`b2e170cc6f8226891f572dce6ce453abba836aaa`. Task 304 was documentation-only,
so the corrected live evidence remains applicable and the destructive live gate
is not rerun.

Evidence: Windows 10 Professional `10.0.19045` build 19045 x64; Rust/Cargo
`1.96.0`; Git `2.54.0.windows.1`; certified `codex-cli 0.149.0`; executable
SHA-256 `14b7e6b2356e82d1d9275579eaa588757b4e0a501b65dcc19fccdf77bd83dc00`;
Prepare Tool/native `0/0`; Confirm `1/1`; final proof `ReviewedSuccess`;
terminal status `renamed_verified`; HEAD/index/refs preserved; Commit
authorization pending before and after Prepare and invalidated after
Started/effect; model lifecycle/MCP/Process Plugin `0`; Codex reaped. Marker:
`RAH_REVIEWED_RENAME_HOSTEXPLICIT_LIVE_OK`.

This is connected-current host-driven evidence only. It does not claim
model-selected rename certification or Linux/macOS live parity.

## Deterministic validation

The required gates were run sequentially and passed:

```text
cargo fmt --check
cargo check --workspace
cargo test --workspace
cargo clippy --workspace --all-targets --all-features -- -D warnings
git diff --check
cargo metadata --no-deps --format-version 1
cargo build -p rah-desktop --release
node --check crates/rah-desktop/frontend/status.js
node crates/rah-desktop/frontend/status_authority_test.js
node crates/rah-desktop/tauri_permission_test.js
```

Results: `cargo fmt --check` PASS; `cargo check --workspace` PASS;
`cargo test --workspace` PASS (all executed tests passed; host-only/live tests
remained ignored); `cargo clippy --workspace --all-targets --all-features --
-D warnings` PASS; `git diff --check` PASS; desktop release build PASS;
frontend syntax, authority, and Tauri permission checks PASS. The focused
rename evidence remained unchanged: reviewed preparation 24 passed, ordinary
rename 263 passed, and the four Windows collision regressions rejected before
native effect with zero native attempts.

`cargo metadata --no-deps --format-version 1` reports 13 workspace packages,
13 workspace members, all package versions `0.25.0`, and edition 2024. The
Cargo.lock diff contains only the 13 internal workspace version transitions;
dependency versions, sources, checksums, features, and edges did not drift.
The destructive Windows reviewed-rename live gate was not rerun because
production and test behavior are unchanged.

## Explicit nonclaims and audit

No production Rust behavior, frontend behavior, Tauri permission, ADR
status/contract, provider protocol, Trusted Profile schema, Codex baseline,
public Tool schema, HostExplicit eligibility, or dependency version changed.
The release claims no race-free TOCTOU, rollback, OS sandbox, network
isolation, generic filesystem mutation, HostExplicit `repo.commit`, provider
Tools, or model-selected reviewed rename certification.

## Candidate and publication prohibition

The final release-candidate commit SHA and exact-head CI result after push are
recorded in the closure of this plan. The candidate must be pushed normally,
with local `HEAD == origin/master` and a clean worktree after successful
exact-head CI. Do not amend, force-push, reset, rebase, merge, tag, publish,
or create a GitHub Release. Later publication is a separate Task 306 gate.

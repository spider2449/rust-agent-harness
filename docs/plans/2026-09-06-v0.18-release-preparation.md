# Task 219 — RAH v0.18.0 Release Preparation

## Starting checkpoints

- Historical milestone-readiness decision: `95a460714115591b4aab2a287f9ba287a3336de1`
  (`docs: audit v0.18 trusted profile persistence milestone`), with exact-head
  CI `34004857262` PASS.
- Release-preparation source checkpoint: `c42fd36bfe31762a746eda2cf1c87838d5dfd7fe`
  (`fix: parse paginated cleanup tags`).
- `c42fd36` is an accepted, unrelated GitHub Actions maintenance commit exactly
  one commit after the Task 218 audit. Its sole semantic change is `jq -r` to
  `jq -s -r` in `.github/workflows/actions-cleanup.yml` so paginated tag
  responses parse correctly. It changes no Rust product code, Cargo state,
  authority/security semantics, Codex/Desktop behavior, Task 207/217 evidence,
  or v0.18 product claim.
- Starting pre-flight: `HEAD == origin/master == c42fd36...`; clean worktree;
  no `v0.18.0` tag or GitHub Release. The manually dispatched Actions Cleanup
  run `34005428084` completed successfully on `c42fd36`.

## Scope

Prepare, but do not publish, RAH v0.18.0 for **Inert Trusted Profile Persistence
and Explicit Restore**:

- bump all 13 workspace packages from `0.17.0` to `0.18.0`, retaining edition
  `2024` and no dependency drift;
- update the current release documentation and add the v0.18 release gate;
- preserve all completed implementation/evidence history without changing Rust
  product behavior or authority semantics;
- run the release validation and exact-head CI before completing Task 219.

## Required claims

- Desktop persists one host-owned Trusted Profile source-path preference as
  inert desired state. Startup is remembered-not-restored and does not select,
  validate, compose, spawn, advertise, or restore provider authority.
- Explicit Restore fresh-loads and statically validates the current source
  without spawning. Explicit Connect/reconnect remains the sole activation
  boundary and fresh-loads the source before composition.
- Forget removes only the durable preference; it does not alter an already
  connected composition. Profile generation participates in currentness.
- The certified `codex-cli 0.149.0` baseline v2 store has an explicit
  host-only repair workflow for legacy/invalid stores. It is not automatic
  repair, download, migration, or product authority.
- Windows persistence lifecycle evidence is PASS after Task 217A repaired the
  baseline prerequisite. Task 217's initial result remains INCONCLUSIVE in the
  chronology; Task 217A remains supporting baseline maintenance; Task 217 final
  scope is PASS.

## Non-claims and preserved limitations

Task 207 remains **INCONCLUSIVE / externally blocked for the model-selected
external Tool execution sub-gate**. This preparation must not claim
model-selected MCP/Process Plugin execution, live external Tool lifecycle or
external-effect handling, Linux Desktop persistence lifecycle certification,
absence of ambient provider effects, OS sandboxing, network isolation,
rollback, active-provider auto-restore, hot reload, credential persistence,
network MCP, provider installation/update, or new generic shell/filesystem/Git
authority.

## Validation record

The final Task 219 record must include actual results for:

- `scripts/codex-baseline.ps1 verify 0.149.0`;
- `cargo fmt --check`;
- `cargo check --workspace`;
- `cargo test --workspace` (with the known Windows foreign-owner fixture debt
  recorded accurately if reproduced);
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`;
- `cargo metadata --no-deps --format-version 1` confirming 13 packages,
  `0.18.0`, edition `2024`;
- `git diff --check`; and
- exact-head GitHub CI after the preparation commit.

## Completed local validation

- `scripts/codex-baseline.ps1 verify 0.149.0`: PASS.
- `cargo fmt --check`: PASS.
- `cargo check --workspace`: PASS.
- `cargo test --workspace`: completed with the existing Windows foreign-owner
  diagnostic fixture debt retained as visible non-product technical debt.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: PASS.
- `cargo metadata --no-deps --format-version 1`: 13 packages, every one
  `0.18.0`, edition `2024`.
- `git diff --check`: PASS.

## Completion boundary

Task 219 creates the preparation commit `docs: prepare RAH v0.18.0 release`,
pushes it only after local validation, and requires `HEAD == origin/master`, a
clean worktree, and successful exact-head CI. It creates no `v0.18.0` tag and
no GitHub Release. Task 220 remains not started.

## Subsequent publication record

Task 220 subsequently published the prepared immutable release without creating
a master commit. The release commit is
`bd0d237b8cda8f4cebdf56d2e3a792b5dd79ba2e`; annotated tag `v0.18.0` has tag
object `e2ac5affc58984dd8ffad5a4d0b9f0de4780d8f0` and peels to that commit.
GitHub Release numeric ID `383437699` is published at
https://github.com/spider2449/rust-agent-harness/releases/tag/v0.18.0.
Task 219 exact-head CI `34006233351` and tag-triggered CI `34006623521` passed.

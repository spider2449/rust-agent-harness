# Task 089 — RAH v0.8 Release Preparation

Date: 2026-08-25

## Starting audit milestone

- Audited release-ready HEAD:
  `da4667fba3f5e8097a160ed7ec8926ca2b0e1d4f`.
- Task 088 exact-head Ubuntu CI: `32803452752` (`completed / success`).
- Target version: `0.8.0` for all 11 workspace packages, edition 2024.

## Release-preparation work

- Update workspace package metadata and Cargo lockfile package metadata without
  dependency upgrades.
- Update README and CHANGELOG release wording for bounded `repo.create-file`.
- Move the v0.8 release gate from milestone-audit state to release-preparation
  state while preserving ADR 0013 and all capability boundaries.

## Required validation

- Verify Cargo metadata, formatting, focused `rah-tools`, `rah-runtime-codex`,
  and `rah-cli` checks/tests/clippy, then the full deterministic workspace
  gates and `git diff --check`.
- Verify the committed `codex-cli 0.149.0` baseline and SHA-256, isolated
  `CODEX_HOME`, `gpt-5.4`, `medium` reasoning, and expected configuration
  fingerprint.
- Run baseline/live-gate script regression and three fresh certified
  `repo.create-file` live validations before committing.

## Final publication outcome

- Task 089 release-preparation commit:
  `0b12d5448dcea89b158e4941e7b741b7539c8894`.
- Exact-head Ubuntu CI run `32804191964` completed successfully.
- The three fresh post-commit certified live validations passed after the
  exact-head CI result.
- Task 090 created immutable annotated tag `v0.8.0`; its tag object is
  `198eccd34a8ae76b9235736c3d1a64173692c351` and it peels to the Task 089
  release commit.
- Tag CI run `32804873958` completed successfully.
- GitHub Release `RAH v0.8.0` (ID `376127345`) was published at
  `2026-08-25T03:29:45Z`:
  <https://github.com/spider2449/rust-agent-harness/releases/tag/v0.8.0>.

The release is complete. Task 091 owns the documentation-only post-release
cleanup; it must preserve the immutable release tag and release commit.

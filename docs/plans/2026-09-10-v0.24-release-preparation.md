# Task 287 — RAH v0.24.0 Release Preparation

## Status

PREPARATION COMPLETE — PUBLISHED LATER

This plan records Task 287 release preparation only. Task 287 created no
`v0.24.0` tag, GitHub Release, artifact publication, or post-release cleanup.
Task 288 later published the exact immutable prepared source with no commit.
The current immutable published release is v0.24.0; v0.23.0 is the prior
immutable published release. v0.25 work is not started.

## Authoritative starting point

- Repository root was resolved dynamically with `git rev-parse --show-toplevel`.
- Starting worktree was clean.
- `git fetch origin --tags` completed.
- `HEAD == master == origin/master ==
  4197d90cd04493b562db7ae9be315074f9320433`.
- Baseline subject: `docs: audit v0.24 reviewed deletion milestone`.
- Direct parent: `3a465d3b5010f86e606e3e3a0281d20774aacbc8`.
- Task 286 CI: run `34454136984`, push/master,
  `4197d90cd04493b562db7ae9be315074f9320433`, completed/success.
- Task 286 acceptance:
  `RAH v0.24 REVIEWED DELETION MILESTONE READY FOR RELEASE PREPARATION`.

The immutable v0.23.0 publication was verified unchanged: annotated tag object
`5a27d84c269a0d57a8a6ad5f0fca89c06379e711`, peeled source
`05527ce10cc088bbaa09fc6792e0f26f6c85ac2b`, and GitHub Release ID
`385352229`. The v0.24.0 tag and GitHub Release were absent before editing.

## Scope and versioning intent

The exact release theme is **HostExplicit Reviewed File Deletion
(`repo.delete-file`)**. This task changes the workspace package version from
`0.23.0` to `0.24.0` and regenerates the workspace package records in
`Cargo.lock`. All 13 workspace packages remain edition 2024. No dependency,
resolver, member, public API, production Rust behavior, frontend behavior,
test behavior, authority, ADR, or workflow change is intended.

The release-facing documentation records the separation:

```text
ADR 0017 = sole underlying repository file-deletion mutation authority
ADR 0021 = generic HostExplicit coordinator/currentness/ticket/D2/provenance boundary
ADR 0025 = reviewed human deletion HostExplicit boundary
```

It records the typed `{path}` zero-effect Prepare, complete escaped review,
opaque ticket-only Confirm, currentness and retained-preparer revalidation,
exact definition/permission membership, D2-before-Started, authorized
dispatch through the current ToolRegistry, independent effect/no-effect
proof, five statuses, and Windows one-attempt/no-polling semantics. The exact
ten eligible HostExplicit Tools and all reviewed bounds/nonclaims are carried
forward without broadening authority.

## Evidence carried forward

Task 285 Windows destructive live certification is carried forward and was not
rerun. Its live-certified code/test SHA is
`e6cb63436541fe653ba0ea4165466aaa822bc206`; its final docs-only evidence SHA
is `3a465d3b5010f86e606e3e3a0281d20774aacbc8`; and its host-attested marker is
`RAH_DELETE_FILE_HOSTEXPLICIT_LIVE_OK`. The evidence records Prepare `0/0`
Tool/native, Confirm `1/1`, `deleted_verified`, one unstaged deletion, intact
index/HEAD/branch/refs/unrelated staged state, Commit authorization
invalidation before Started, duplicate/activity-ID rejection, zero model/MCP/
Process Plugin activity, and owned cleanup PASS. No destructive live test is
rerun by Task 287.

## Historical completion and later publication

Task 287 preparation completed at the immutable source
`2d4cf0e4d89a461f54250ff81ad9808929d854f0`.

- Task 287 exact-head CI: run `34456482581`, branch `master`, event `push`,
  status `completed`, conclusion `success`.
- Task 288 later published that exact immutable source with no commit.
- Annotated tag object: `5708cfd0078ba1593e86077bf88c0dddaa03c1bf`.
- Tag peeled source: `2d4cf0e4d89a461f54250ff81ad9808929d854f0`.
- Tag CI: run `34457322318`, branch `v0.24.0`, event `push`, status
  `completed`, conclusion `success`.
- GitHub Release ID: `386128676`.
- Published: `2026-09-10T08:53:24Z`.

These publication objects were created after Task 287 preparation and are
recorded here as historical facts. This preparation plan did not authorize or
perform publication.

## Expected exact changed scope

```text
Cargo.toml
Cargo.lock
CHANGELOG.md
README.md
docs/ARCHITECTURE.md
docs/SECURITY.md
docs/RAH_V0.24_RELEASE_GATE.md
docs/plans/2026-09-10-v0.24-release-preparation.md
```

No Rust, frontend, test, ADR, workflow, or other dependency manifest file is
in scope. If version synchronization requires any additional file, work must
stop before commit rather than silently expand the scope.

## Validation results

The version update was performed with normal Cargo tooling. `cargo check
--workspace` passed and updated only the 13 internal workspace package version
records in `Cargo.lock`; third-party dependency versions, sources, checksums,
features, and edges were unchanged. `cargo metadata --no-deps --format-version
1` reports 13 packages, all version `0.24.0`, all edition `2024`.

The final validation results and exact Task 287 commit/CI identities are
recorded below after execution:

```text
cargo fmt --check = PASS
cargo check --workspace = PASS (initial version-update check)
cargo test -p rah-tools = PASS (231 passed)
cargo test -p rah-desktop = PASS (215 passed, 9 ignored)
cargo test --workspace = PASS
cargo clippy --workspace --all-targets --all-features -- -D warnings = PASS
node --check crates/rah-desktop/frontend/status.js = PASS
node crates/rah-desktop/frontend/status_authority_test.js = PASS
git diff --check = PASS
cargo metadata --no-deps --format-version 1 = PASS (13 packages, version 0.24.0, edition 2024)
cargo build -p rah-desktop --release = PASS
git diff --name-only / status --short --untracked-files=all = exact eight expected paths
git diff -- Cargo.toml Cargo.lock = PASS; only root 0.23.0 -> 0.24.0 and 13 internal lockfile records changed; no dependency drift
```

The ignored test `tests::windows_live_desktop_hostexplicit_delete_file` is
excluded and was not rerun.

## Commit, push, and stop boundary

- Preferred commit: `docs: prepare RAH v0.24.0 release`.
- First Task 287 commit must be a direct child of
  `4197d90cd04493b562db7ae9be315074f9320433`.
- Before push, fetch `origin --tags` again and require origin/master remains
  the baseline and `v0.24.0` remains absent.
- Push normally; do not amend, force-push, merge, rebase, reset, tag, or
  create a Release.
- Require exact-head push CI for the final Task 287 SHA with branch `master`,
  event `push`, status `completed`, and conclusion `success`.
- After CI, require `HEAD == master == origin/master == <FINAL_SHA>` and a
  clean worktree.

Final Task 287 commit sequence:

```text
2d4cf0e4d89a461f54250ff81ad9808929d854f0
```

Final exact-head CI:

```text
34456482581 — master — 2d4cf0e4d89a461f54250ff81ad9808929d854f0 — push — completed — success
```

Task 287 stopped after exact-head CI and final release-prepared verification.
Task 288 performed publication later. Task 289 is a separate documentation-only
post-release cleanup; its descendant commit is not the v0.24.0 release source.
V0.25 work remains outside this plan and is not started.

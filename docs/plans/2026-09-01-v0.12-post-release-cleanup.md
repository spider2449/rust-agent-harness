# Task 155 - v0.12 post-release state cleanup

## Starting state

- Starting `HEAD` and `origin/master`:
  `d1c1cd470fd337f141abb9675fb4642ccd2e00b0`.
- Starting worktree: clean; `.vscode/` remains untouched.
- Immutable release commit:
  `d1c1cd470fd337f141abb9675fb4642ccd2e00b0`.
- Annotated tag: `v0.12.0`.
- Tag object: `4d002a8bc67b1877e692bb0aafd764fc5eb47b65`.
- Candidate CI: `33479033004`, PASS.
- Tag CI: `33479751261`, PASS.
- GitHub Release: `RAH v0.12.0`, published
  `2026-09-01T06:58:38Z`, `draft=false`, `prerelease=false`.

## Scope

- Update `CHANGELOG.md`, `docs/RAH_V0.12_RELEASE_GATE.md`, and the Task 153
  plan from release-preparation wording to accurate released historical state.
- Preserve Task 151 non-blocking observability evidence gaps and all closed
  v0.12 authority conclusions.
- Do not change version, dependencies, ADRs, authority, production code,
  release tag, or GitHub Release.

## Validation and completion

- Run the required serial workspace checks, documentation diff check, metadata
  inspection, and frontend syntax check.
- Create one post-release cleanup commit after validation. It is a descendant
  of the release commit and is not the `v0.12.0` tag target.
- Push normally and require a new successful exact-head CI run for that cleanup
  commit.

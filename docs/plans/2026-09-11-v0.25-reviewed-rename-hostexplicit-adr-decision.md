# Task 297 — RAH v0.25 Reviewed File Rename/Move HostExplicit Decision Gate

## Scope

This task accepts ADR 0026 only after an independent ordinary ADR 0018 gate.
It does not implement a reviewed rename preparer, Desktop integration,
frontend UI, Tauri permission changes, HostExplicit eligibility, or live
certification.

## Ordinary foundation gate

The audit started from the authoritative clean checkpoint:

- `HEAD`: `2715a784a4e9c1b6dbc3612093f561039251e6b0`;
- direct parent: `1bdbd5d86ef33c72eafedb8d526511b9b007c612`;
- exact-head CI: `34561746201`, pass.

The current ordinary implementation was independently re-read at exact HEAD.
The gate found no new material ADR 0018 defect. It covers canonical
repository-root identity, supported directory-form `.git` identity, Git
executable identity, source and destination eligibility, source HEAD/index
equality, destination HEAD/index absence, source/destination ancestry,
nested-repository and mount-like boundary handling, same-volume support,
parent and source identities, destination absence, Windows alias/case
behavior, one native no-replace attempt, exact `known_no_effect` proof,
independent `renamed_verified` proof, and conservative `uncertain`
classification. The implementation remains check-and-revalidate best effort
and makes no race-free handle-bound claim.

Focused validation at the gate:

```text
cargo test -p rah-tools repository_rename_file::tests
36 passed, 0 failed on Windows
```

The ordinary gate therefore passed and ADR 0026 was accepted. The reviewed
route narrows ordinary admission by requiring complete bounded strict-UTF-8
review and rejecting ignored-but-absent destinations; it does not amend ADR
0018 or change the ordinary Tool.

## Decision

ADR 0026 accepts the human/host route:

```text
typed {source_path, destination_path}
 -> zero-effect Prepare
 -> complete host-derived review
 -> opaque process-local single-use ticket
 -> ticket-only Confirm
 -> currentness/generation and capability revalidation
 -> D2
 -> reviewed Commit authorization invalidation
 -> HostExplicit Started
 -> authorized_tool_dispatch
 -> current ToolRegistry
 -> existing repo.rename-file / ADR 0018
 -> independent reviewed post-effect proof
```

The exact decision and all bounds, privacy rules, proof classes, non-effects,
nonclaims, and deferred eligibility are recorded in ADR 0026.

## Validation

The documentation-safe and workspace checks passed:

- `git diff --check`;
- `cargo fmt --check`;
- `cargo check --workspace`;
- `cargo test --workspace`;
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`.

No Windows live certification was run or claimed for this task.

## Deferred work

The next task is the narrow backend reviewed-rename preparation,
revalidation, and post-proof foundation. No Desktop/frontend integration,
permission expansion, live certification, or current allowlist change starts
automatically.

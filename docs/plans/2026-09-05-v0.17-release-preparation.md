# Task 209 - RAH v0.17.0 Release Preparation

## Objective

Prepare and verify the exact `RAH v0.17.0` release candidate for the Desktop
Host-Selected Trusted Profile External Provider Integration milestone. This
task stops before tag creation and GitHub Release publication.

## Starting checkpoint

- Starting `HEAD`: `3681f1d8f4b515ca6f30e3151b6ae53913523dbb`.
- Starting `origin/master`: `3681f1d8f4b515ca6f30e3151b6ae53913523dbb`.
- Starting commit: `docs: audit v0.17 external provider milestone`.
- Task 208 exact-head CI: `33956624643` PASS.
- Worktree was clean before this task.

## Scope

- Bump the shared RAH workspace version from `0.16.0` to `0.17.0`, retaining
  Rust edition `2024` for all 13 packages.
- Update `Cargo.lock` only for the resulting RAH workspace package version
  references; preserve established Task 207A dependency state and avoid
  third-party drift.
- Update CHANGELOG, README, architecture/security release-facing wording.
- Create `docs/RAH_V0.17_RELEASE_GATE.md` and this preparation plan.
- Do not change production Rust, provider, permission, authority, or Codex
  baseline behavior.

## Release claims and limits

The candidate documents the provider-only Desktop overlay, inert static
selection, Connect/reconnect activation, shared composition, local stdio MCP,
Process Plugin, exact Tool-set/schema admission, host-selected permissions,
fresh registry merge, duplicate fail-closed admission, provider lifecycle
ownership/cleanup, sanitized external descriptors, Configured/Effective/
Advertised/Current separation, conservative external-effect handling, and
hardened live-certification infrastructure.

Task 207 remains INCONCLUSIVE / externally blocked for model-selected external
Tool execution. The candidate must not claim Task 207 PASS, live-certified
external MCP or Process Plugin execution, Windows model-driven external
execution, or real external ToolStarted/ToolFinished repository refresh.
The limitation is accepted by Task 207C / Task 208.

The certified Codex baseline remains `codex-cli 0.149.0` with SHA-256
`14b7e6b2356e82d1d9275579eaa588757b4e0a501b65dcc19fccdf77bd83dc00`; the
current live-gate model is `gpt-5.6-terra`. Codex 0.153.4 is research-only
compatibility evidence and is not certified.

The release limits remain explicit: no network MCP or Streamable HTTP MCP,
provider installation/update, PluginManager expansion, profile hot reload,
active-provider auto-restore/persistence, generic shell/process/filesystem/
Git authority, OS sandboxing, network isolation, rollback, claims of no
ambient external effects, or Linux external-provider live certification.

## Validation record

Actual results:

- `cargo fmt --check`: PASS.
- `cargo check --workspace`: PASS.
- `cargo test --workspace`: 156 `rah-desktop` tests passed, 1 failed, and 2
  ignored; the one failure is the known Task 208 Windows foreign-owner fixture.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: PASS.
- `git diff --check`: PASS.
- `cargo metadata --no-deps --format-version 1`: PASS; 13 packages, all
  `0.17.0`, edition `2024`.
- Focused provider/composition/runtime tests: PASS — composition 4 tests;
  MCP 31 integration plus 1 unit; Process Plugin 15 integration plus 6 unit;
  Codex runtime 82 unit/bridge, 6 architecture, and 11 live-gate contract
  tests, with 1 ignored.
- Focused Desktop tests: 156 passed, 1 failed, and 2 ignored, with the same
  known fixture failure.
- Frontend Node syntax checks and `status_authority_test.js`: PASS.
- `cargo build -p rah-desktop --release`: PASS.

The exact failure was
`tests::hardened_git_environment_requires_host_pinned_safe_directory_for_foreign_owner_diagnostic`,
asserting `the diagnostic must reproduce Git's protected ownership refusal` at
`crates/rah-desktop/src/main.rs:6438`. The exact focused rerun reproduced it.
It is the pre-existing Windows/Git fixture portability issue recorded by Task
208; no product code was changed. The validation is acceptable under the Task
208 classification, with exact-head CI still required for this preparation
commit. No model-selected external Tool live run was manufactured.

## Dependency audit

Compare `Cargo.toml` and `Cargo.lock` with the starting checkpoint and record:

- additions: none expected;
- removals: none expected;
- source/version drift: none expected outside RAH workspace package versions;
- Cargo.lock: only 13 RAH workspace package version references should change.

## Completion boundary

After acceptable validation, commit only intended Task 209 files as:

`docs: prepare RAH v0.17.0 release`

Push `master`, require exact `HEAD == origin/master`, and wait for CI whose
head SHA exactly equals the preparation commit. Reconfirm 13 packages, version
`0.17.0`, edition `2024`, no unexpected dependency drift, prepared/not-yet-
released wording, no `v0.17.0` tag or GitHub Release, and a clean worktree.

Task 209 is complete only after exact-head CI passes. The next task is Task
210 - v0.17.0 Release Publication; it is not started automatically.

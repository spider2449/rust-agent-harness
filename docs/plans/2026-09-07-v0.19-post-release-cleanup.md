# Task 233 — RAH v0.19.0 Post-Release State Cleanup

## Result

RAH v0.19.0 is released.

This cleanup commit occurs AFTER the immutable v0.19.0 release commit.

The tag remains on:
`88463787d14e32a3f6692385161b59474bff1dea`

The post-release cleanup commit is NOT part of v0.19.0 source identity.

## Starting release identity

- Release commit: `88463787d14e32a3f6692385161b59474bff1dea`
- Annotated tag: `v0.19.0`
- Tag object: `24fff2dbae77d6fbd281bf9f4ac34b855c116e3a`
- Peeled target: `88463787d14e32a3f6692385161b59474bff1dea`
- Starting `HEAD`: the immutable release commit.
- Starting `origin/master`: the immutable release commit.
- Starting worktree: clean.

## Publication verification

- GitHub Release: `RAH v0.19.0`
- Numeric ID: `383770772`
- Node ID: `RE_kwDOT-U4RM4W3-CU`
- Tag: `v0.19.0`
- URL: https://github.com/spider2449/rust-agent-harness/releases/tag/v0.19.0
- draft: `false`
- prerelease: `false`
- Publication date: `2026-09-07`

## Immutable tag verification

The annotated tag object is `24fff2dbae77d6fbd281bf9f4ac34b855c116e3a` and
the peeled target is `88463787d14e32a3f6692385161b59474bff1dea`. Local and
remote tag identities were verified before documentation changes.

## Documentation cleanup

The release-candidate wording was converted to released historical state in
the changelog, README, architecture, security model, and release gate. The
Task 231 plan retains its preparation chronology and now includes the separate
Task 232 publication record. No publication object or release asset is edited.

## Product / authority contract

The accepted ADR 0020 `repo.create-branch` contract is unchanged. Branch
creation remains a separate host-owned authority at captured attached `HEAD`.
Branch creation != branch switching. Execute is only the outer dispatch gate;
model request != authorization. No generic or remote Git/ref/history authority
is added, and Trusted Profile/provider metadata cannot grant branch authority.

## Windows host-driven certification

Windows host-driven Desktop `repo.create-branch` authority/effect path is
certified. The successful fresh branch name and OID were internally asserted
but not printed on the successful output path.

## Model-selected limitation

Model-selected `repo.create-branch` dispatch was not observed in two bounded
Codex live attempts and is not certified. This is not a model-selected PASS
claim, and no live branch effect or model-selection experiment is rerun.

## Preserved limitations

- Linux live branch certification is not established.
- The fresh branch name/OID echo limitation is preserved; values are not
  invented.
- Uncertain effects are not replayed, retried, rolled back, or compensated.
- No rollback guarantee, OS sandbox claim, or network isolation claim is made.
- Process supervision != OS sandboxing.
- No race-free TOCTOU claim is made.

## Task 207 status

Task 207 remains INCONCLUSIVE / externally blocked for the model-selected
MCP / Process Plugin Tool execution sub-gate. It is unchanged by this
documentation-only cleanup.

## Codex baseline

The certified baseline remains exactly `codex-cli 0.149.0` with SHA-256
`14b7e6b2356e82d1d9275579eaa588757b4e0a501b65dcc19fccdf77bd83dc00`.
The current live-gate model remains `gpt-5.6-terra`.

## Validation

The required sequential validation is:

- `cargo fmt --check`
- `cargo check --workspace`
- `git diff --check`
- `cargo metadata --no-deps --format-version 1`

The metadata audit must show 13 packages, all version `0.19.0`, with edition
2024. `Cargo.toml` and `Cargo.lock` must have no diff and no dependency drift.
No branch live effect, model-selection experiment, tag-triggered CI rerun, or
full effectful certification is part of this task.

## Cleanup commit

The cleanup commit SHA is assigned at commit time and is recorded in Task 233
completion evidence. It is later than the immutable v0.19.0 release commit and
does not move the tag.

## Exact-head CI

The cleanup commit must pass exact-head CI after push.

The final run ID is reported in Task 233 completion evidence.

## Final repository state

After push, `master` is expected to point to the later documentation-only Task
233 cleanup commit while `v0.19.0` continues to point to the immutable release
commit. The worktree is expected to be clean and `HEAD == origin/master`.

## Next task

Task 234 — RAH v0.20 Scope and Authority Roadmap.

Task 234 is research only and is not started automatically by Task 233.

# Task 244 — RAH v0.20.0 Post-Release State Cleanup

## Status

DOCUMENTATION UPDATED — AWAITING CLEANUP COMMIT / CI

This task is documentation-only and occurs after immutable Task 243
publication. It does not change the v0.20.0 source identity.

## Starting release identity

- Release commit: `0d2b4c46be5d879b70c660501f9b7236040a7a61`.
- Task 242 exact-head CI: `34090581488` PASS.
- Annotated tag: `v0.20.0`.
- Tag object: `bc3addb4f41914a97a91521eb01c4726ae3c2c73`.
- Peeled target: `0d2b4c46be5d879b70c660501f9b7236040a7a61`.
- Tag-triggered CI: `34091553715` PASS.

## Immutable publication verification

The local and remote annotated tag object is
`bc3addb4f41914a97a91521eb01c4726ae3c2c73`. Its peeled target is
`0d2b4c46be5d879b70c660501f9b7236040a7a61`. The tag remains permanently on
the Task 242 release commit.

## GitHub Release verification

The published GitHub Release is `RAH v0.20.0`, numeric ID `383875194`, node ID
`RE_kwDOT-U4RM4W4Xh6`, tag `v0.20.0`, `draft=false`, `prerelease=false`,
published at `2026-09-07T06:39:07Z`:

https://github.com/spider2449/rust-agent-harness/releases/tag/v0.20.0

## Documentation cleanup

The current documentation now identifies v0.20.0 as released and current,
v0.19.0 as the prior release, and the release gate as a historical record.
The Task 242 plan retains its preparation chronology and has a separate
subsequent publication record.

## Product contract

The exact first-release HostExplicit eligible Tool set remains:

- `fs.read`
- `repo.file-info`
- `repo.status`
- `repo.diff`
- `repo.diff-staged`
- `repo.create-branch`

External provider Tools remain unavailable. `repo.commit` and worktree
authoring HostExplicit remain unavailable.

## ADR 0021 / D2 boundary

The preserved boundary is:

```text
Human Host action
 -> connected-current composition
 -> backend eligibility
 -> D2
 -> ToolRegistry
 -> existing capability authority
```

The runtime/model path remains distinct. Host explicit dispatch is not runtime/
model dynamic dispatch and neither route manufactures capability authority.
D2 retains exact current Tool-definition and explicit permission checks.

## HostExplicit eligibility

Eligibility remains host-owned, connected-current, typed, and limited to the
six first-party Tools listed above. There is no generic `ToolName`/arbitrary
JSON route, no authority amplification, and no model-text trigger.

## Windows live certification

Windows Desktop connected-current explicit Host Tool invocation is certified
for `repo.status` and `repo.create-branch` through the production HostExplicit
backend path. No model request or model Tool lifecycle was used.

Task 240 branch: `rah-host-explicit-live-18d2efa09d330900-2`.
OID: `e6b376c26b0d97e12c1be2c7981aecc32974e29c`.

No live rerun is part of Task 244.

## Deterministic-only HostExplicit coverage

These eligible Tools were deterministically verified but not separately live
certified:

- `fs.read`
- `repo.file-info`
- `repo.diff`
- `repo.diff-staged`

## Model-selected limitations

Model-selected `repo.create-branch` dispatch remains not certified from the
historical Task 229 evidence. v0.20 provides the explicit HostExplicit product
alternative; it does not claim fixed model Tool selection.

## Task 207 status

Task 207 remains unchanged: model-selected MCP / Process Plugin Tool execution
was not established. External provider Tools are not HostExplicit eligible in
v0.20.

## GUI / platform limitations

Real GUI mouse-click automation was not part of the Task 240 Windows live
certification. Linux HostExplicit live behavior and macOS HostExplicit live
behavior are not certified.

## Security nonclaims

The documentation continues to make no claim of post-start HostExplicit
cancellation, replay, rollback, authority or ticket persistence, conversation
injection, an OS sandbox, network isolation, or race-free TOCTOU behavior.
Process supervision is not OS sandboxing.

## Codex baseline

- codex-cli: `0.149.0`
- codex.exe SHA-256: `14b7e6b2356e82d1d9275579eaa588757b4e0a501b65dcc19fccdf77bd83dc00`
- model: `gpt-5.6-terra`
- reasoning: `medium`

No live rerun is part of Task 244.

## Validation

Task 244 validation is documentation-only and is run sequentially:

- `cargo fmt --check`
- `cargo check --workspace`
- `git diff --check`
- `cargo metadata --no-deps --format-version 1`

Metadata must report 13 packages, all version `0.20.0`, edition 2024.
`Cargo.toml` and `Cargo.lock` must have empty diffs and no dependency drift.
No live tests, model prompts, or Tool experiments are run.

## Files changed

Exactly these seven documentation files are in scope:

1. `CHANGELOG.md`
2. `README.md`
3. `docs/ARCHITECTURE.md`
4. `docs/SECURITY.md`
5. `docs/RAH_V0.20_RELEASE_GATE.md`
6. `docs/plans/2026-09-07-v0.20-release-preparation.md`
7. `docs/plans/2026-09-07-v0.20-post-release-cleanup.md`

Rust, tests, Cargo, dependencies, ADRs, frontend production files, workflows,
permissions, and scripts remain unchanged.

## Cleanup commit

Commit message: `docs: mark RAH v0.20.0 released`

The cleanup commit is after the release tag and must have parent
`0d2b4c46be5d879b70c660501f9b7236040a7a61`. The cleanup commit SHA is left to
the completion report.

## Exact-head CI

The normal `master` push CI must complete successfully for the exact cleanup
commit. Its run ID is left to the completion report; no second documentation
commit is created solely to record that ID.

## Final repository state

After push, `HEAD == origin/master` at the cleanup commit and the worktree is
clean. The v0.20.0 tag object and peeled target remain unchanged, and the tag
does not point to the cleanup commit.

## Next task

Task 245 — RAH v0.21 Scope and Authority Roadmap. It is not started by Task
244 and must remain research-only.

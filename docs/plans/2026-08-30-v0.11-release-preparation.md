# Task 141 — RAH v0.11.0 release preparation

## Starting checkpoint

- Required Task 140 audit checkpoint: `15420bcffa0a2bb2155f7f691a9af17f07ca0894`.
- Required Task 140 exact-head CI: `33295380139` — PASS.
- Only local item: untracked `.vscode/`, left untouched.

## Intended scope

Prepare only the immutable `0.11.0` release candidate for the audited bounded
host-reviewed `repo.commit` authority. No product capability, authority,
dependency, Codex baseline, ADR, or Desktop source change is permitted.

## Candidate changes

- Bump the workspace and all 12 resolved packages from `0.10.0` to `0.11.0`.
- Refresh only workspace-local `Cargo.lock` package-version records.
- Add the v0.11 release-candidate changelog and release gate.
- Correct only stale current-candidate documentation where needed.

## Version bump

`[workspace.package]` remains Rust edition `2024` and changes only its version
to `0.11.0`. Cargo metadata must show 12 packages, all `0.11.0`, edition 2024.

## Changelog

The candidate entry records bounded `repo.commit`, Trusted Profile composition,
deterministic and Windows certified live evidence, and the explicit authority,
platform, rollback, Task 120, and transport limitations. It does not claim a
tag or GitHub Release.

## Release gate

`docs/RAH_V0.11_RELEASE_GATE.md` starts as **RELEASE PREPARATION — NOT YET
RELEASED**. Its Task 141 commit and exact-head CI remain pending in the
committed document.

## Deterministic gates

Run serially: formatting, workspace check, workspace tests with `-j 1`, strict
Clippy, diff check, metadata, frontend syntax, and the Desktop release build.
Cargo lockfile and manifest diffs are audited for dependency churn.

## Desktop release artifact

Build `rah-desktop` in release mode and record the canonical output path, size,
timestamp, and whether adjacent `sqlite3.dll` exists. SQLite remains bundled;
no adjacent DLL is expected.

## Windows live evidence reused

Task 139 evidence is reused, not rerun: certified Windows Codex `0.149.0` with
same-version code-mode host, harmless and `repo.commit` lifecycle `1 / 1 / 1`,
`committed_verified`, and disposable fixture OID
`13c200c5c772b3e4a0eceb0a2364981c849313e0`.

## Dependency audit

No Cargo dependency was introduced by the v0.11 milestone or Task 141. The
post-bump dependency delta must be **NONE**.

## Deferred scope

ADR 0016 remains authoritative. No generic Git, branch/ref, network,
credential, linked-worktree, submodule, generic filesystem/shell/process,
rollback, OS-sandbox, or network-isolation authority is added. Task 120 remote
llama generation remains **DEFERRED / NOT VALIDATED**; transport confinement
remains **NOT CLAIMED**.

## Immutable release procedure

After the release-preparation commit is pushed and receives its own green
exact-head deterministic-validation CI, it is the proposed immutable v0.11.0
tag target. Task 142, not Task 141, may create the annotated tag and GitHub
Release. No tag or GitHub Release is created by this task.

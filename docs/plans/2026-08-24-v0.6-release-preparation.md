# Task 067: RAH v0.6.0 release preparation

## Baseline

- Task 066 release-ready commit: `2c52b64680e04b2c3f8aff644d3083f12153dc52`.
- Released baseline: RAH v0.5.1.
- Required Codex live baseline: exactly `codex-cli 0.149.0`.
- Task 066 Ubuntu CI: `32684390117`, completed successfully.

## Release-preparation change set

- Bump workspace package metadata and generated lockfile package entries from
  `0.5.1` to `0.6.0`.
- Add the v0.6 changelog entry and a release gate recording scope, authority,
  deterministic/live evidence, and platform wording.
- Preserve the already accurate README, architecture, security, and milestone
  audit documents unless a factual release-facing correction is necessary.

## Scope and authority

v0.6 releases only the fixed read-only observer Tools `repo.file-info`,
`repo.status`, `repo.diff`, and `repo.diff-staged`, alongside the separately
governed existing `repo.patch` capability. This task adds no behavior,
authority, dependency, crate, public API, protocol, profile-schema, or ADR
change. `profile_version = 1` remains unchanged.

## Gates

- Run full deterministic workspace formatting, check, test, clippy, diff, and
  metadata gates, plus focused `rah-tools`, `rah-cli`, and `rah-runtime-codex`
  tests.
- Recheck exactly `codex-cli 0.149.0`, then run the fresh-fixture Windows live
  observer bridge and verify every observer invocation, lifecycle, unchanged
  repository, and cleanup rather than accepting its marker alone.
- Commit, repeat clean-tree deterministic/metadata gates, push the
  release-preparation commit, and wait for required Ubuntu CI.

## Deferred

Task 067 must not create or move `v0.6.0`, push a tag, or publish a GitHub
Release. Those immutable publication actions are deferred to Task 068 after the
release-preparation commit and its required CI are confirmed green.

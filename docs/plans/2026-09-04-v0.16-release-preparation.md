# Task 198 — RAH v0.16.0 Release Preparation

## Scope and starting baseline

Prepare the v0.16.0 release candidate through workspace metadata and
release-facing documentation only. Starting HEAD and `origin/master` were
`aeb9df521522817e62edca661417b02be095a3fe`; the worktree was clean. Task 197
milestone audit and exact-head CI `33847133836` both passed.

No Rust, frontend, Tauri capability, ADR, dependency, authority, tag, or
GitHub Release change is in scope.

## Preparation

- Bump all 12 workspace packages from `0.15.0` to `0.16.0`; retain edition
  `2024`.
- Update Cargo.lock only with normal workspace package version changes.
- Update CHANGELOG, README, architecture/security documentation, and add the
  v0.16 release gate.
- Preserve the Task 191–197 evidence chain and the initial Task 196
  INCONCLUSIVE chronology.

## Evidence and release status

The milestone is Effective Authority Review UX: a read-only, sanitized,
generation-aware review of the current Desktop host-composed Tool inventory.
Configured, effective, advertised, and individual request authorization remain
distinct; no new model-accessible authority was added and no new ADR was
required.

Windows live certification is PASS using certified `codex-cli 0.149.0`, with
the hashes recorded in the release gate. Linux live certification is not
established. No live JSONL evidence existed; the truthful observation is that
no chat prompt was sent and no Tool activity was visually observed while
repository postconditions remained unchanged. Deterministic evidence supports
the zero-side-effect behavior independently.

## Validation record

Actual results:

- `cargo fmt --check` — PASS.
- `cargo check --workspace` — PASS.
- `cargo test --workspace` — PASS; 2 tests ignored, 0 failed.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` — PASS.
- `git diff --check` — PASS.
- `cargo metadata --no-deps --format-version 1` — PASS; 12 packages, all
  `0.16.0`, edition `2024`, unchanged workspace members.
- `node --check crates/rah-desktop/frontend/status.js` — PASS.
- `node --check crates/rah-desktop/frontend/status_authority_test.js` — PASS.
- `node crates/rah-desktop/frontend/status_authority_test.js` — PASS.
- `cargo build -p rah-desktop --release` — PASS.

Cargo.lock diff audit — PASS; only the 12 RAH workspace package versions
changed, with no dependency package/version/source drift.

Exact changed files:

- `Cargo.toml`
- `Cargo.lock`
- `CHANGELOG.md`
- `README.md`
- `docs/ARCHITECTURE.md`
- `docs/SECURITY.md`
- `docs/RAH_V0.16_RELEASE_GATE.md`
- `docs/plans/2026-09-04-v0.16-release-preparation.md`

## Release boundary

Task 198 creates no tag and no GitHub Release. The v0.15.0 commit and annotated
tag object remain immutable. After the single coherent release-preparation
commit is pushed and its exact-head CI passes, the next task is Task 199 —
RAH v0.16.0 Release, which owns tag and GitHub Release creation.

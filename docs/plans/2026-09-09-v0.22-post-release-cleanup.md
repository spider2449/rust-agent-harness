# Task 267 — RAH v0.22.0 Post-Release State Cleanup

## Status

DOCUMENTATION-ONLY POST-RELEASE CLEANUP

This task updates the current release-facing documentation after publication
of RAH v0.22.0. It does not modify Rust, Cargo, frontend, tests, scripts,
workflows, ADRs, permissions, profiles, providers, the GitHub Release, or the
immutable release tag.

## Starting release identity

- Starting `HEAD == master == origin/master`:
  `76895b4067c38167f3c41a3536f6616cffa8293a`.
- Starting worktree: clean.
- Immutable release source / peeled target:
  `76895b4067c38167f3c41a3536f6616cffa8293a`.
- Annotated tag: `v0.22.0`.
- Tag object: `eb91039521eac190efc85ef57ee2a5c28ca4e9fe`.
- Task 265 exact-head CI: `34300321835` PASS.
- Tag CI: `34301369809` PASS.

## GitHub Release verification

- Release: `RAH v0.22.0`.
- Release ID: `385176161`.
- Tag: `v0.22.0`.
- Published: `2026-09-09T02:02:09Z`.
- Draft: `false`.
- Prerelease: `false`.
- Assets: `0`.

## Cleanup scope

Exactly these seven documentation files are in scope:

1. `CHANGELOG.md`
2. `README.md`
3. `docs/ARCHITECTURE.md`
4. `docs/SECURITY.md`
5. `docs/RAH_V0.22_RELEASE_GATE.md`
6. `docs/plans/2026-09-09-v0.22-release-preparation.md`
7. `docs/plans/2026-09-09-v0.22-post-release-cleanup.md`

No Cargo, Rust, frontend, test, script, workflow, ADR, permission, profile,
provider, tag, or GitHub Release file or object is in scope.

## Documentation result

- v0.22.0 is identified as the current immutable published release.
- v0.21.0 is identified as the prior immutable published release.
- The release gate is recorded as `RELEASED — HISTORICAL RECORD`.
- The Task 265 preparation chronology remains historical and accurate.
- The Task 265 subsequent publication record is appended to its plan.
- The exact eight HostExplicit Tools and all security boundaries remain
  unchanged.
- The Task 263 Windows evidence is carried forward without a live rerun or
  model prompt, and remains host-driven rather than model-selected evidence.

## Release-source invariant

The immutable v0.22.0 release source forever remains:

`76895b4067c38167f3c41a3536f6616cffa8293a`

The Task 267 cleanup commit is NOT the v0.22.0 release source. The annotated
tag remains `eb91039521eac190efc85ef57ee2a5c28ca4e9fe` and peels to the release
source, never to the cleanup commit. ADR 0014 remains the underlying mutation
authority, and ADR 0023 remains the reviewed HostExplicit boundary.

## Preserved capability and nonclaims

The exact HostExplicit eligible Tools remain:

- `fs.read`
- `repo.file-info`
- `repo.status`
- `repo.diff`
- `repo.diff-staged`
- `repo.create-branch`
- `repo.patch`
- `repo.edit-files`

`repo.create-file`, `repo.delete-file`, `repo.rename-file`,
`repo.create-directory`, `repo.commit`, MCP Tools, Process Plugin Tools, and
unknown Tools remain ineligible. Prepare remains zero-effect; Confirm remains
opaque ticket-only with immediate currentness revalidation and D2. The
operation remains non-atomic with six result classes and no retry, replay,
prefix continuation, rollback, automatic Stage/Commit, or authority-ticket
persistence/resume. No generic `fs.write`, `shell.exec`, structural
HostExplicit authoring, model-selected HostExplicit certification, network
isolation, OS sandboxing, race-free TOCTOU, or Linux/macOS production live
parity is claimed.

## Windows evidence carried forward

Task 263 final Attempt 2 remains the committed connected-current production
evidence:

- Platform: Windows 11 IoT Enterprise LTSC x64.
- Codex: `0.149.0`.
- SHA-256:
  `14b7e6b2356e82d1d9275579eaa588757b4e0a501b65dcc19fccdf77bd83dc00`.
- Model: `gpt-5.6-terra`; reasoning: `medium`.
- Prepare: 0 Tool / 0 native attempts.
- Confirm: 1 Tool / native attempts `1/1/1/1`.
- Caller order: `d.txt, b.txt, a.txt, c.txt`.
- Backend/effect order: `a.txt, b.txt, c.txt, d.txt`.
- Generations: `[1, 0, 0, 1]`.
- Coordinator/chat: Idle / Idle.
- Model lifecycle: all zero.
- MCP: 0. Process Plugin: 0.
- Marker: `RAH_MULTI_FILE_HOSTEXPLICIT_LIVE_OK`.

This is host-driven evidence, not a model-selected HostExplicit certification
claim. Task 267 does not rerun live certification or submit a model prompt.

## Validation

Run sequentially without ignored live tests:

- `cargo fmt --check`
- `cargo check --workspace`
- `cargo test --workspace`
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- `node --check crates/rah-desktop/frontend/status.js`
- `node crates/rah-desktop/frontend/status_authority_test.js`
- `cargo build -p rah-desktop --release`
- `git diff --check`
- `cargo metadata --no-deps --format-version 1`
- `cargo test -p rah-tools`
- `cargo test -p rah-desktop`

Metadata must show exactly 13 workspace packages, all version `0.22.0`, all
edition 2024. `git diff -- Cargo.toml Cargo.lock` must be empty. No live test
or model prompt is part of this task.

## Cleanup commit and exact-head CI

Preferred commit message: `docs: mark RAH v0.22.0 released`.

The cleanup commit is created after validation with direct parent
`76895b4067c38167f3c41a3536f6616cffa8293a`. Its SHA and the exact-head CI run
ID/result are recorded in Task 267 completion evidence after they exist. No
second documentation commit is created solely to record those values.

Before push, `origin/master` must still equal the immutable release source and
the remote `v0.22.0` tag must still have the recorded object and peeled target.
After push, cleanup CI must be completed successfully for `master` at the exact
cleanup commit SHA, followed by verification that `HEAD == origin/master` and
the worktree is clean. The tag is never moved or recreated, and the GitHub
Release is never edited.

## Final state

The Task 267 cleanup commit is a later documentation-only descendant of the
release source. It is NOT the v0.22.0 release source. The immutable release
source forever remains `76895b4067c38167f3c41a3536f6616cffa8293a`.

Task 267 stops after exact-head cleanup CI. It does not start v0.23 work.

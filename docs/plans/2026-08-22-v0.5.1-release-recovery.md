# v0.5.1 release recovery plan

Date: 2026-08-22

Task: 055R

Status: Complete

## Purpose

Recover the published v0.5.0 release from its required Ubuntu CI clippy-only
failure without rewriting public history. `v0.5.0` remains immutable: its
annotated tag peels to `b1f0fb4a903a59e0b5c23ca107d7508ebcbd8786` and its GitHub
Release remains published.

## Recovery implementation

Recovery commit `6aae5b1fb710cb9d84fc1bcc51bddb9d1be9e22e` changes only
`crates/rah-tools/src/repository_worktree_patch.rs`. `std::fs::File` was
unconditionally imported but only used by `File::from_raw_handle` inside the
Windows-only `capture_windows_file_identity` function. The import is now gated
with `#[cfg(windows)]`; `fs` and `OpenOptions` remain unconditional.

No `repo.patch` semantics, authority, path security, replacement behavior,
test behavior, public API, dependency, crate edge, profile, Codex behavior, or
ADR changed. No new regression test was needed because the cfg-correct import
is directly proved by the existing required Ubuntu clippy job.

## Evidence and release path

The recovery commit's pushed GitHub Actions run `32573592369` passed the one
required Ubuntu job: formatting, workspace check, workspace tests, and clippy.
The former `unused import: File` lint is gone. There is no GitHub Windows job;
the established Windows live `repo.patch` gate was rerun successfully with
exact `codex-cli 0.149.0` and no Unix live Codex claim.

The follow-up v0.5.1 release-preparation commit changes only workspace version,
the 11 corresponding internal lockfile versions, this recovery record, the
v0.5.1 release-gate audit, and CHANGELOG wording. Required CI must pass for
that exact commit before an annotated `v0.5.1` tag is created and pushed.

## Validation

At version `0.5.1`, the complete deterministic suite, focused `rah-tools`,
`rah-runtime-codex`, and `rah-cli` suites, Cargo metadata, and `git diff --check`
passed. The rerun live gate completed with `RAH_REPO_PATCH_LIVE_OK`, one
`repo.patch` invocation/replacement attempt, unchanged index/HEAD/refs and
unrelated content, reaped app-server, removed fixture, and restricted
Codex-owned capabilities disabled.

## Next step

Push the separate v0.5.1 release-preparation commit, wait for required CI on
that exact commit, then create/push the annotated `v0.5.1` tag and publish
`RAH v0.5.1`. Do not modify `v0.5.0` or begin post-release cleanup here.

# Task 325 — RAH v0.26.0 Release Preparation

Date: 2026-09-15
Task type: release preparation only
Status: **COMPLETED — HISTORICAL PREPARATION RECORD**

## Checkpoint and scope

- starting SHA: `21bc151efab969dcd725842459c049f2d6dd74c8`;
- direct parent of the preparation commit: `21bc151efab969dcd725842459c049f2d6dd74c8`;
- Task 323: **READY — v0.26 MULTI-REPOSITORY MILESTONE READY FOR WINDOWS
  LIVE CERTIFICATION**;
- Task 322: Recommendation A — stop feature expansion for v0.26;
- Task 324-C-R4 live source: `e59319166dc0776458c9c0bee61f4c73d9d6399c`;
- Task 324-C-R4 result: **PASS — WINDOWS HOST-DRIVEN MULTI-REPOSITORY
  AUTHORITY/EFFECT CERTIFICATION PASSED**;
- final certification documentation head:
  `21bc151efab969dcd725842459c049f2d6dd74c8`;
- release-preparation SHA: `8b22b18739a54b752ce11e791fe380e522c10049`;
- no production, test, frontend, permission, ADR, provider-schema, model, or
  runtime behavior change is authorized.

The release theme is **Explicit Multi-Repository Membership and Active-Only
Repository Switching**. Multiple repositories may be explicitly admitted into
one process-local membership set, but only one human/host-selected active
repository receives executable repository authority. Inactive members remain
inert; switching A→B→A creates fresh active-only composition and invalidates
stale repository-bound state.

## Version and metadata

The workspace version changes from `0.25.0` to `0.26.0` across exactly 13
packages. Rust edition remains 2024. Cargo.lock is expected to change only the
13 internal RAH package-version records. No dependency version, checksum,
source, feature flag, or dependency-graph drift is allowed.

The exact HostExplicit set remains 11:

```text
fs.read
repo.file-info
repo.status
repo.diff
repo.diff-staged
repo.create-branch
repo.patch
repo.edit-files
repo.create-file
repo.delete-file
repo.rename-file
```

`repo.create-directory`, `repo.commit`, MCP Tools, Process Plugin Tools,
fixtures/diagnostics, and unknown/provider-defined Tools remain ineligible.
Stage/Unstage remain separate host index authority and Commit remains separate
repository-bound reviewed authority.

## Changed files

The intended exact Task 325 changed-file scope was:

```text
Cargo.toml
Cargo.lock
CHANGELOG.md
README.md
docs/ARCHITECTURE.md
docs/SECURITY.md
docs/RAH_V0.26_RELEASE_GATE.md
docs/plans/2026-09-15-v0.26-release-preparation.md
```

All changes are version metadata or release documentation. No additional file
is in scope without an explicit explanation that it is metadata/docs-only.

## Evidence record

The release-facing documents record Task 323 READY, Task 322 scope freeze,
Task 324-B's disposition, and Task 324-C-R4's host-driven PASS. They preserve
the original Task 324 BLOCKED result, Task 324-A's external/generic
model-selection block, Task 324-B Recommendation B, and Task 324-D's observer
correction as historical evidence layers.

Task 324-C-R4 marker:

```text
RAH_V026_HOST_DRIVEN_MULTI_REPOSITORY_LIVE_OK
```

The required nonclaim is retained verbatim:

```text
MODEL-SELECTED DYNAMIC TOOL DISPATCH NOT ESTABLISHED UNDER THE APPROVED GPT-5.6-TERRA LIVE GATE.
```

The Windows host-driven evidence is limited to Microsoft Windows 10
Professional build 19045 x64, Rust/Cargo 1.96.0, Git 2.54.0.windows.1,
Codex 0.149.0, and SHA-256
`14b7e6b2356e82d1d9275579eaa588757b4e0a501b65dcc19fccdf77bd83dc00`.
It does not establish Windows 11/Linux/macOS parity or provider-specific live
certification. R4 ran with MCP 0 and Process Plugin 0.

Essential R4 results are: two admissions, zero duplicate successes, A→B→A,
A reads 2, B reads 1, cross-repository read rejected, zero successful switch
while Connected, A/B runtime ownership and Disconnect/reap PASS, old A Stage
effects 0, B Stage 1, B Unstage 1, no-union registry PASS, fresh-host restored
authority none, current-run cleanup PASS, new leaked roots 0, remaining
app-server 0, model requests 0, model Tool requests 0, model-selected switches
0, automatic commits 0, network Git 0, and A/B Git integrity PASS.

The five historical Task 324 roots remain outside release scope. R4 proved
pre-existing count 5, post-run count 5, and new leaked roots 0; it did not
delete them or treat them as v0.26 artifacts.

## Validation

The required validation passed before commit:

```text
cargo fmt --check
cargo check --workspace
cargo test --workspace -- --test-threads=1
cargo clippy --workspace --all-targets --all-features -- -D warnings
git diff --check
cargo metadata --no-deps --format-version 1
cargo build -p rah-desktop --release
```

Results:

- `cargo fmt --check`: PASS.
- `cargo check --workspace`: PASS.
- `cargo test --workspace -- --test-threads=1`: PASS; all executed tests
  passed. Desktop: 250 passed/12 ignored; Codex runtime: 83 passed/1
  ignored; rah-tools unit: 288 passed/0 ignored; all other workspace targets
  passed with zero failures. Explicit live/host-only probes remained ignored.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
  PASS.
- `git diff --check`: PASS.
- `cargo metadata --no-deps --format-version 1`: PASS; 13 packages, all
  `0.26.0`, all edition 2024, zero `0.25.0` package versions.
- `cargo build -p rah-desktop --release`: PASS; Desktop compiled as
  `rah-desktop v0.26.0`.
- Frontend syntax, status-authority, and repository-membership checks: PASS.
- Tauri permission tests: PASS.
- Focused Task 321-I, Task 321-G/E/C, Task 320, Task 318, Task 315,
  Repository Commit, Stage, Unstage, HostExplicit exact-set, and managed-state
  cleanup coverage: PASS in the serialized workspace suite.

The external live certification was not rerun because the release changes are
docs/version metadata only; Task 324-C-R4 remains valid.

The final record must verify 13 packages at `0.26.0`, edition 2024, no
external dependency drift, Desktop release build version `0.26.0`, and no
Rust/test/runtime source change.

## Release and publication checks

The immutable v0.25 identity is preserved:

- tag `v0.25.0`;
- tag object `ea3c31aaf5190b632d7ef86387f7aff6004ae664`;
- peeled source `a8b4d7b92f545a37d2ef2c8eae224f91c9d939c4`;
- GitHub Release ID `387406579`, `draft=false`, `prerelease=false`.

Task 325 explicitly stopped before publication and did not create, push, or
publish either artifact. Task 326 later created the annotated tag and GitHub
Release without a repository commit. The immutable publication facts are:

- release source: `8b22b18739a54b752ce11e791fe380e522c10049`;
- annotated tag: `v0.26.0`;
- tag object: `f60ee2465c1819b59b5671f20387b74734ace019`;
- peeled target: `8b22b18739a54b752ce11e791fe380e522c10049`;
- release-preparation CI: `34965819523` — PASS;
- tag CI: `34966821012` — PASS;
- GitHub Release ID: `389111343`, name `RAH v0.26.0`;
- published: `2026-09-15T12:07:53Z`, `draft=false`, `prerelease=false`;
- assets: 0.

Task 327 updates documentation after publication. Its cleanup commit is a
later docs-only descendant and is not the v0.26.0 release source.

## Claim and nonclaims

The final claim is:

**READY — RAH v0.26.0 RELEASE PREPARATION COMPLETE**

This historical Task 325 claim authorized only separately authorized Task 326
publication, which has since completed. It does not claim model-selected Tool
dispatch, workspace-wide authority, parallel active repositories,
cross-repository operations, persisted membership, member removal,
provider-selected routing, HostExplicit `repo.commit`, network Git, rollback,
network isolation, race-free TOCTOU, OS sandboxing, or Windows 11/Linux/macOS
live parity. Uncertain external effects are not replayed.

## Historical follow-on

Task 326 independently verified the final preparation SHA and exact-head CI,
created the annotated `v0.26.0` tag targeting exactly that SHA, verified its
peeled target and tag CI, and created the non-draft/non-prerelease GitHub
Release without a repository commit. Task 327 is the separate post-release
documentation cleanup.

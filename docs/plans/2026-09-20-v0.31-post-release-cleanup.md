# Task 372 - RAH v0.31.0 Post-Release State Cleanup

Date: 2026-09-20

Status: **COMPLETE - DOCS-ONLY HISTORICAL CLEANUP**

## Scope

Task 372 converts the prepared/not-yet-published v0.31.0 documentation into an
accurate historical release record. It changes documentation only. It does not
change Rust production behavior, Cargo metadata, dependencies, public APIs,
ADRs, authority, permissions, or product semantics.

Task 370 prepared the immutable source and stopped before publication as
designed. Task 371 published that source. This Task 372 cleanup commit is a
later descendant and is **not** the v0.31.0 release source.

## Immutable publication record

- Release source: `c4d21deac56b034c2df6c96060b29ae259bf536f`.
- Annotated tag: `v0.31.0`; annotation: `RAH v0.31.0`.
- Tag object: `732ee33ff06ef456a8fa0d7d1b9af6a246e01980`.
- Peeled target: `c4d21deac56b034c2df6c96060b29ae259bf536f`.
- Release-preparation CI: `35447671491` - PASS.
- Tag CI: `35481008480` - PASS.
- GitHub Release ID: `392273508`; name: `RAH v0.31.0`.
- Published: `2026-09-20T01:20:20Z`.
- Release state: `draft=false`; `prerelease=false`; assets=`0`.

The GitHub Release metadata value `target_commitish=master` does not supersede
or modify the annotated tag identity. `v0.31.0^{}` continues to peel to the
immutable release source above.

## Preserved product, authority, and nonclaim state

The released capability remains `repo.search` with closed `path` and `text`
modes, tracked files only, selected active worktree only, and literal,
case-sensitive host-side matching. Its classification remains `ReadOnly` /
`RepositoryObservation` / `Execute` / repository-bound. HostExplicit remains
exactly 11.

No ADR, PermissionLevel, dependency, mutation authority, network authority,
persistence authority, or generic Git/process/filesystem authority is added.
All 13 workspace packages remain `0.31.0` with Rust edition `2024`.

The accepted release nonclaims remain unchanged: `codex-cli 0.149.0` was
unavailable during Task 369; ambient `0.155.1` was not substituted; real
model-selected Codex inference was not certified; and Windows symlink live
behavior was not certified. Publication does not convert these nonclaims into
claims.

## Validation

Task 372 validation records:

- `git diff --check` - PASS.
- `cargo check --workspace` - PASS.
- `cargo metadata --no-deps --format-version 1` - PASS; 13 packages, all
  `0.31.0`, edition `2024`.
- Documentation diff review - PASS; no Rust, Cargo, dependency, API, ADR, or
  authority changes.
- Final tag verification - PASS; `v0.31.0^{}` remains
  `c4d21deac56b034c2df6c96060b29ae259bf536f`.

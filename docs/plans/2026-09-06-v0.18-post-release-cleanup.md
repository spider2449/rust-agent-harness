# Task 221 - RAH v0.18.0 Post-Release State Cleanup

## Result

RAH v0.18.0 is released. This commit occurs AFTER the immutable v0.18.0 release commit. The tag remains on `bd0d237b8cda8f4cebdf56d2e3a792b5dd79ba2e`; the post-release docs commit is NOT part of v0.18.0 source identity.

## Starting release identity

- Release: `RAH v0.18.0`.
- Release commit and peeled tag target: `bd0d237b8cda8f4cebdf56d2e3a792b5dd79ba2e`.
- Annotated tag: `v0.18.0`; tag object: `e2ac5affc58984dd8ffad5a4d0b9f0de4780d8f0`.
- GitHub Release: numeric ID `383437699`, node ID `RE_kwDOT-U4RM4W2suD`, https://github.com/spider2449/rust-agent-harness/releases/tag/v0.18.0.
- Task 219 exact-head CI: `34006233351` PASS. Tag-triggered CI: `34006623521` PASS.

## Publication verification

The published release is named `RAH v0.18.0`, targets `v0.18.0`, and remains published (`draft=false`, `prerelease=false`).

## GitHub Release note encoding correction

The pre-cleanup release body contained `?` encoding artifacts. Only the release body was corrected with ASCII-safe `Remembered - not restored` wording and `->` lifecycle arrows; release identity was not changed.

## Immutable tag verification

`v0.18.0^{tag}` remains `e2ac5affc58984dd8ffad5a4d0b9f0de4780d8f0` and `v0.18.0^{}` remains `bd0d237b8cda8f4cebdf56d2e3a792b5dd79ba2e`.

## Documentation cleanup

CHANGELOG, README, architecture, security, and release-gate status now record the historical released state. The Task 219 preparation plan retains its original chronology and adds the subsequent Task 220 publication record.

## Preserved product claims

v0.18 persists one host-selected Trusted Profile source path as inert desired state. Restart is remembered-not-restored; explicit Restore is non-spawning; only explicit Connect or reconnect may activate providers; Forget does not alter an already-connected composition.

## Preserved limitations

Linux Desktop persistence lifecycle is not live-certified. RAH does not claim absence of ambient external-provider effects, OS sandboxing, network isolation, or rollback.

## Task 207 status

Task 207 remains INCONCLUSIVE / externally blocked for the model-selected external Tool execution sub-gate. v0.18 does not establish model-selected MCP or Process Plugin execution, real external ToolRequested/Started/Finished, or model-driven external effect handling.

## Task 217 chronology

Task 217 initial was INCONCLUSIVE because a legacy v1 certified baseline store lacked the current v2 companion host contract. Task 217A was PASS for the explicit host-only repair prerequisite. Resumed/final Task 217 was PASS for the Windows Trusted Profile persistence/provider activation lifecycle.

## Codex baseline

Certified baseline: `codex-cli 0.149.0`; manifest `v2`; live model `gpt-5.6-terra`. `codex.exe` SHA-256: `14b7e6b2356e82d1d9275579eaa588757b4e0a501b65dcc19fccdf77bd83dc00`. `codex-code-mode-host.exe` SHA-256: `3c6726ab12b8de7c0bccecf4551af686d9dbe1b9fcdaee90bd66f60837943ac2`. `0.153.4` remains research-only and is not the release baseline.

## Validation

Completed: `cargo fmt --check`, `cargo check --workspace`, and `git diff --check` passed. `cargo metadata --no-deps --format-version 1` confirmed 13 packages at `0.18.0`, edition `2024`; Cargo files and dependency resolution did not change.

## Cleanup commit

Commit documentation-only cleanup as `docs: mark RAH v0.18.0 released`.

## Exact-head CI

Record the Task 221 cleanup commit exact-head CI after push.

## Final repository state

`master` is the post-release cleanup commit; `v0.18.0` remains the immutable release tag on the release commit.

## Next task

Task 222 - RAH v0.19 Scope and Authority Roadmap (research-only; not started).

# Task 352 — RAH v0.29 Active Repository Close Live Certification

Date: 2026-09-17

Status: **PASS WITH EXPLICIT NONCLAIMS**.

## Scope and source identity

Task 352 began at the required clean checkpoint
`edc4e95676df63be60a123cfadf3f771e3a47110`, with `HEAD == origin/master`.
Task 351 production behavior base is
`99f44aebb89fb697573f16f40a849290d5a9cf09`; Task 351's audit/docs head was
`edc4e95676df63be60a123cfadf3f771e3a47110`, with exact-head CI `35184979132`
PASS and verdict PASS WITH NARROW HARDENING.

The final certified source is
`c9cff2afac40f9d518310da38ba4c9e158af1641`. Its only Task 352 source change
is the ignored Windows certification harness in
`crates/rah-desktop/src/main.rs`; no production/frontend behavior, dependency,
ADR, persistence/schema, release preparation, or authority surface changed.
Exact-head CI `35212728847` passed on this SHA:
https://github.com/spider2449/rust-agent-harness/actions/runs/35212728847.
The final Windows live test was built and executed on this exact source SHA,
with a clean checkout and `HEAD == origin/master`.

The source-only validation sequence passed the complete Task 352 gate set:

- `cargo fmt --all -- --check`
- `cargo check --workspace`
- `cargo test --workspace -- --test-threads=1`
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- `cargo build -p rah-desktop --release`
- `cargo metadata --no-deps --format-version 1`
- All requested Node checks/tests for status, membership, authority,
  remembered workspaces, and Tauri permissions.
- `git diff --check`

The workspace reported 13 packages, all version `0.28.0`, Rust edition 2024;
HostExplicit remained exactly 11. No dependency drift was introduced.

## Harness iteration record

The following earlier source attempts were not accepted as certification. Each
had exact-head CI PASS, but its live run either selected no test or stopped on
a certification-harness expectation/setup issue; none emitted the Task 352
marker. Each correction remained in the Windows test-only harness:

| Source | Exact-head CI | Live outcome |
|---|---:|---|
| `0aa8b74` | `35196401073` PASS | The `--exact` filter selected zero tests; no live claim. |
| `520ec04` | `35198206041` PASS | Harness expected a consumed Commit review to remain in workflow state. |
| `ef657a7` | `35200126006` PASS | Baseline comparison included pre-Close Commit-review index setup. |
| `3a1c8a9` | `35202196617` PASS | P1/P2/P3 passed; startup-counter check was placed after explicit repository work. |
| `b755903` | `35203959525` PASS | Startup counter tracking included explicit certification setup operations. |
| `89137e7` | `35205682763` PASS | Connected Close assertion group needed component diagnostics. |
| `926670a` | `35207541399` PASS | Connected guard/Disconnect passed; harness counted 14 total Tools but expected 11 eligible HostExplicit Tools. |
| `ac18029` | `35209284854` PASS | Connected case passed; broad restart predicate failed. |
| `d274d67` | `35211022257` PASS | Component diagnostics showed all restart authority/state checks passed; the harness incorrectly expected A's repository-scoped transcript in the fresh neutral presentation. |

On the final run, the neutral transcript was empty and non-resumable, as
required for startup without repository admission. The complete persistent
storage snapshot and transcript presentation were already proven byte/
semantically unchanged across each measured Close interval.

## Live execution record

The final opt-in command was
`cargo test -p rah-desktop task_352_windows_active_repository_close_live_certification -- --ignored --test-threads=1 --nocapture`, gated by
`RAH_RUN_V029_ACTIVE_REPOSITORY_CLOSE_LIVE=1`. It used the production Tauri
Desktop backend and lifecycle paths, two disposable real Windows Git
repositories outside the development checkout, and the certified Codex
baseline only for the connected-runtime precondition. It finished with one
test passed and emitted:

`RAH_V029_ACTIVE_REPOSITORY_CLOSE_LIVE_OK`

### Environment

- Windows 10 Professional, build 19045, x64.
- `rustc 1.96.0 (ac68faa20 2026-05-25)`.
- `cargo 1.96.0 (30a34c682 2026-05-25)`.
- Git `2.54.0.windows.1`.
- Ambient PATH Codex: `codex-cli 0.154.0`, not used as the certified runtime.
- Certified baseline: available, selected from `CertifiedBaseline`,
  `codex-cli 0.149.0`, SHA-256
  `14b7e6b2356e82d1d9275579eaa588757b4e0a501b65dcc19fccdf77bd83dc00`.
- Desktop `0.28.0`; HostExplicit 11; workspace 13 packages at `0.28.0`,
  edition 2024.

### Scenario results

| Scenario | Live result |
|---|---|
| P1 disconnected Close A | Passed. A and B remained admitted; IDs/order and membership generation stayed fixed; A became inactive, no active member or `DesktopRepository` remained, repository generation advanced `1 → 2`, workflow/Commit/HostPrepared/conversation executable state reset, and Effective Authority had no repository generation or Tool inventory. |
| Repeated Close | Returned bounded `no_active`; membership/repository generations, conversation epoch, catalog, transcript storage, and Git stayed unchanged. |
| Reactivate A | Passed with the same process-local member ID, unchanged membership generation, fresh `DesktopRepository`, generation `2 → 3`, disconnected runtime, fresh workflow observation, and stale pre-Close selectors/review/ticket rejected. |
| P2 stale A request after switching to B | Returned bounded `active_changed`; B stayed current at generation 4 with unchanged repository, workflow, Effective Authority, and Git state. No retry or automatic B Close. |
| P3 Close B | Passed; both members remained admitted/inactive; repository generation advanced `4 → 5`; zero active repository authority. |
| Stage reservation | Close returned `repository_effect_busy`; reservation owner and A remained current; index unchanged; no native Stage operation from Close. Existing owner path released the reservation. |
| Unstage reservation | Same live result for Unstage: busy, owner preserved, index unchanged, no native Unstage from Close. |
| Reviewed Commit | A real no-effect review/authorization was withdrawn; old review stayed stale; no Commit, HEAD, ref, index, or worktree mutation. |
| HostPrepared | The production coordinator was placed in a safe prepared state through `PreparedHostInvocation::for_test`; Close invalidated it and the ticket became unusable. No dispatch/native attempt. A full HostExplicit Prepare/Confirm flow was not executed. |
| Connected runtime | Certified Codex 0.149.0 Connect succeeded. Close returned `connected_or_runtime_busy` and preserved A/runtime/authority. Explicit Disconnect reaped the attributable app-server child, reached `NotConnected`, left provider activation absent, and did not reconnect. A subsequent explicit Close succeeded. |
| Restart boundary | Fresh Desktop state had zero members, active repository, repository Tools, Commit authority, Stage/Unstage state, HostExplicit preparation, provider activation, runtime repository binding, or executable conversation context. Remembered candidates remained descriptive. Neutral transcript was empty and not resumable. |

Repository generations were `1 → 2` (P1), `2 → 3` (A reactivation), `3 → 4`
(switch to B), and `4 → 5` (P3). Membership generation never changed during
Close/reactivation/switch. Effective Authority after each successful Close
showed no selected/current executable repository or repository Tool inventory.

### Integrity, privacy, cleanup, and action counts

Repository A/B captures covered HEAD, status, refs, index SHA-256, sentinel and
controlled worktree hashes, directory existence, and `.git` form. Every
measured successful/rejected Close preserved its immediate pre-Close capture.
Close did not delete or rename repository content. The Commit-review setup's
index cache-tree refresh was recorded before the measured P1 interval; it was
not attributed to Close.

The full host-storage byte snapshot and transcript presentation were unchanged
across Close. Remembered-workspace JSON bytes and candidate presentation were
unchanged. Close results, bounded errors, membership output, and Effective
Authority were scanned for the private repository-path sentinel, roots, `.git`,
Git executable details, filesystem identity, generation text, HostExplicit
ticket, and provider handle; no leak was found.

Measured counts: model requests `0`; model Tool requests `0`; MCP activations
`0`; Process Plugin activations `0`; Git mutations caused by Close `0`;
automatic commits `0`; automatic activations `0`; automatic provider
reconnects `0`; model-selected Close actions `0`. The certified Codex runtime
was used only for Connect/Close-rejection/Disconnect lifecycle and no model
request was issued. No network Git operation was issued by the certification.

Certification-owned processes were reaped; the unique temporary fixture root
was removed. Cleanup used attributable ownership evidence, with no global
process-name kill. Post-run inspection found seven storage-only roots left by
earlier non-certifying attempts; their repository trees were already gone and
their owning test PIDs were no longer running. A recursive removal request for
those exact roots was rejected by execution policy, so they were preserved and
their cleanup remains outstanding. Final marker was emitted only after all
live assertions passed. The final live result is not evidence that these older
attempt artifacts were removed.

### Frontend and nonclaims

The actual Node VM membership test passed the A/17 guard capture, displayed
switch to B/18, and Confirm payload retaining A/17. Tests also passed for no
implicit Disconnect/retry/Activate/Remove/Forget coupling and clearing the
old Close generation after Effective Authority refresh failure. This is not
GUI automation. GUI automation was **NOT EXECUTED**.

A live model-turn/chat-owner busy case and a live `HostRunning` owner were not
created. No model request or external effect was started solely for these
cases. Task 351 deterministic lifecycle coverage remains the evidence for
those boundaries. The HostPrepared case used the coordinator's test-only
reservation seam, not a full HostExplicit Prepare/Confirm flow. This
certification does not establish model-selected
dynamic Tool dispatch, OS sandboxing, network isolation, rollback/replay/
compensation, race-free TOCTOU, cross-platform live parity, linked-worktree
Close behavior, or persistent executable repository membership.

No release preparation was started. The next authorized milestone remains
Task 353, subject to independent acceptance of this Task 352 certification.

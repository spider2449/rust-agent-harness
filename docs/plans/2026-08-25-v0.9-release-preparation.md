# v0.9 Release Preparation

## Scope

Prepare RAH v0.9.0 from authoritative starting commit
`9e9cb5f04f06fbefd206dfaec12a6cbc42f2634b`. The milestone adds no new
authority: it releases the existing bounded `repo.edit-files` capability,
Trusted Profile v1 composition, and Generic Tool Bridge integration.

## Preparation record

- Bump the workspace and all 11 packages from `0.8.0` to `0.9.0`; retain Rust
  edition 2024 and existing dependencies.
- Audit existing Task 094–096 evidence only. The audit confirms bounded
  host-owned multi-file editing, no generic write/shell/Git authority, shared
  mutation lease, preserved raw index/HEAD/refs, and separate `repo.patch` and
  `repo.create-file` capabilities.
- Record ADR 0014 Accepted, Task 095 profile/bridge composition, and Task 096
  Windows live evidence using exactly `codex-cli 0.149.0` with marker
  `RAH_REPO_EDIT_FILES_LIVE_OK`.
- Create the v0.9 release gate, run deterministic validation, and require an
  exact-head Ubuntu CI success after pushing the Task 097 commit.

## Task 098

Use the immutable Task 097 release-preparation commit for the annotated
`v0.9.0` tag, verify tag CI and its peeled commit, then publish the GitHub
Release. No tag or GitHub Release is created during Task 097.

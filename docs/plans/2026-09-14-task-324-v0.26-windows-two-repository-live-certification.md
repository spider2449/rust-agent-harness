# Task 324 — RAH v0.26 Windows Two-Repository Live Certification

Status: BLOCKED — required real Codex model/tool turn did not complete.

This document is the Task 324 plan and certification record. It must contain
only measured evidence from the exact Windows live run and the docs-only
descendant that closes it. Native temporary paths, private selectors,
reservation/ticket values, credentials, and other private identities are
represented by bounded labels or counts.

## Authoritative checkpoint

- Task 323 head: `2f26189f9075b040dbef50f928101c90dda869c0`
- Task 323 direct parent: `9d6a769afd8a73772b291f0f23ccd164ede54101`
- Task 323 exact-head CI: `34797987857` — PASS
- Milestone base: `646dd92099c8e11d4fbf48875186a3013ea58420`
- Accepted architecture: ADR 0027
- Task 322 decision: Recommendation A — stop feature expansion for v0.26

## Certification source and final head

- Live certification source SHA: `3d4e645ca1019a9e0ac19f5e3a23cbe503329ec1`
- Final Task 324 head: the final docs-only closure head reported with this
  record after publication
- Test harness commit: `3d4e645ca1019a9e0ac19f5e3a23cbe503329ec1`
- Documentation commits: `9a0afa8` plus this final metadata-only closure
- Changed paths: `crates/rah-desktop/src/main.rs` (test-only ignored harness)
  and this certification document; no other paths are in scope.

## Scope and method

The required harness is the ignored Windows-only test
`task_324_windows_two_repository_live_certification` in the existing Desktop
test module. It is gated by `RAH_RUN_V026_MULTI_REPO_LIVE=1` and uses the
existing certified Codex live-gate environment. It creates two fresh sibling
repositories beneath one fresh temporary root, uses native Git and the real
Desktop admission, activation, connection, runtime/tool bridge, Stage, and
Unstage paths, and removes only that certification root after all handles are
closed.

The exact command used was:

~~~powershell
$env:RAH_RUN_V026_MULTI_REPO_LIVE = "1"
& .\scripts\codex-live-gate.ps1 -Version '0.149.0' -ExpectedSha256 '14b7e6b2356e82d1d9275579eaa588757b4e0a501b65dcc19fccdf77bd83dc00' -Model 'gpt-5.6-terra' -ReasoningEffort 'medium' -Command { cargo test -p rah-desktop 'tests::task_324_windows_two_repository_live_certification' -- --ignored --exact --nocapture --test-threads=1 }
~~~

One preliminary invocation used the bare function name with `--exact` and
therefore ran zero tests (`252 filtered out`). It was a proven zero-effect
filtering mistake. The corrected command above ran the one ignored test.

## Evidence record

1. Environment: Windows 11 IoT Enterprise LTSC build `26100`, x64; `rustc
   1.98.1 (48a229cea 2026-09-01)`; `cargo 1.98.1 (797e8a9bc 2026-08-05)`;
   native `git version 2.55.0.windows.5`; executable classification
   `regular-native-git-exe`.
2. Codex baseline: `codex-cli 0.149.0`, SHA-256
   `14b7e6b2356e82d1d9275579eaa588757b4e0a501b65dcc19fccdf77bd83dc00`,
   independently verified; the live gate used an isolated temporary Codex
   home and ephemeral auth-file copy. Project baseline remained unchanged.
3. Fixture: two fresh sibling ordinary repositories under one fresh temporary
   root, labeled `<repo-a-temp>` and `<repo-b-temp>`, with distinct commits,
   local-only commit identities, no remotes, hooks, submodules, or network Git.
   Both had the required marker and deliberate unstaged `work.txt` changes.
4. Admission: production admission created exactly two opaque process-local
   members; the initial active member was none, and the A/B presentation did
   not expose native temporary paths. The active-only public state was then
   established through the normal activation route.
5. Duplicate negative: re-admitting A returned bounded
   `RepositoryAlreadyMember`; membership stayed two and did not switch.
6. A active state: production activation selected A; A was active, B inactive,
   repository generation was current, and the A Stage action was captured at
   observation generation `1` without executing it.
7. A registry/boundary: one active-repository registry was composed; no
   member/repository selector appeared in definitions; the A repository-scoped
   sibling read was rejected before B contents were read.
8. Connected A: real Desktop/backend Codex connection reached the connected
   path with MCP `0` and Process Plugin `0`. The exact HostExplicit eligible
   set checked by the harness was `fs.read`, `repo.file-info`, `repo.status`,
   `repo.diff`, `repo.diff-staged`, `repo.create-branch`, `repo.patch`,
   `repo.edit-files`, `repo.create-file`, `repo.delete-file`, and
   `repo.rename-file`; no Commit, create-directory, MCP, Process Plugin, or
   unknown/provider-defined name was admitted.
9. Connected-switch negative: normal A→B activation while Connected returned
   `RepositoryBusy`; successful switches remained `0` at that point, A stayed
   active/current, and no B composition published.
10. A model turn: the real Desktop `send_chat` path was entered with the
    required relative `repo-marker.txt` prompt, but zero activity events were
    observed within the 180-second bound. No `fs.read` Requested/Started/
    Finished lifecycle and no A marker result were proven. Per the task rule,
    this is a mandatory live failure and the harness failed closed.
11. Not reached after the failure: real Disconnect, A→B activation, stale A
    Stage execution, B Stage, B Unstage, B connection/model turn, B→A, and
    final A connection/model turn.
12. HostExplicit stale ticket: not exercised live. The current production
    lifecycle prevents disconnect/switch while a ticket is prepared without a
    new seam; Task 323 deterministic evidence remains authoritative.
13. Counts measured: admissions `2`; duplicate admission successes `0`;
    connected-switch successes `0`; MCP `0`; Process Plugin `0`; model
    repository-switch actions `0`; automatic Commit `0`; network Git `0`.
    B Stage, B Unstage, and completed A/B/A session counts were not certified
    after the model failure.
14. Cleanup: the live runtime was shut down through the existing Desktop
    reaping path. The isolated Codex gate home was removed. Git object files in
    the disposable root were read-only, so final cleanup required clearing
    attributes and deleting only the validated fresh root; verification found
    the root absent. No unrelated Codex process was touched.
15. Fresh-host and persistence checks were not reached by the harness after the
    mandatory model failure. No PASS marker was emitted.
16. Deterministic standard validation and focused regression results are
    recorded in the closure section below.
17. v0.25 identity and exact-head CI are recorded below; publication of this
    blocked result is still pending.

## Limitations and nonclaims

The zero-provider live run does not newly certify provider-specific repository
interaction; host-owned selection and deterministic Task 323 evidence remain
authoritative for that boundary. A live stale HostExplicit ticket is optional
and will be reported as deterministic-only if the current lifecycle prevents
safe preparation/switching without a production seam. Nested-boundary live
parity, rollback, race-free TOCTOU, network isolation, OS sandboxing, and
full executable restart semantics are not claimed unless directly measured.

## Validation and immutable release checks

- `cargo fmt --check`: PASS for the certified source after formatting.
- `cargo check --workspace`: PASS.
- `cargo metadata --no-deps --format-version 1`: PASS.
- `cargo test --workspace -- --test-threads=1`: PASS, `1050 passed`, `0
  failed`, `18 ignored`.
- Frontend/Tauri checks: `node --check crates/rah-desktop/frontend/status.js`,
  `node crates/rah-desktop/frontend/status_authority_test.js`,
  `node crates/rah-desktop/frontend/repository_membership_test.js`, and
  `node crates/rah-desktop/tauri_permission_test.js`: PASS.
- Focused deterministic checks: Task 321-I `3 passed`; Task 321-E `4
  passed`; Task 321-C `1 passed`; Task 320 `3 passed`; Task 318 `1 passed`;
  nested-boundary integration `1 passed`; repository Commit `18 passed`;
  Stage `6 passed`; Unstage `6 passed`; HostExplicit exact allowlist `1
  passed`.
- `cargo build -p rah-desktop --release`: PASS.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
  FAIL on four test-only harness lints (`needless_borrow` at three calls and
  `drop_non_drop` at one state drop). The harness was not changed afterward,
  because doing so would require a fresh exact-source live certification.
- `git diff --check`: PASS before documentation closure.
- Live test command: BLOCKED as described above; result was one failed ignored
  test and no success marker.
- Workspace remains 13 packages, version `0.25.0`, Rust edition 2024.
- No Cargo.lock/dependency drift; no production behavior, frontend, permission,
  ADR, persistence, or removal implementation was added. No dependency edge
  changed.
- v0.25 annotated tag object remains
  `ea3c31aaf5190b632d7ef86387f7aff6004ae664`; peeled source remains
  `a8b4d7b92f545a37d2ef2c8eae224f91c9d939c4`; GitHub Release `387406579`
  remains the immutable published release (`draft=false`, `prerelease=false`).
- Final Task 324 head, remote alignment, exact-head CI, and the documentation
  commit are recorded at closure after the final docs-only metadata commit and
  push.

## Verdict and authorization

BLOCKED — WINDOWS LIVE CERTIFICATION FAILED OR UNAVAILABLE

The required A real Codex model/tool turn did not complete, so the live
certification does not authorize Task 325. The smallest supported next task is
environment/service-resolution or a fresh Task 324 rerun after proving the
prior zero-effect cleanup; no release preparation is authorized.

# Task 278 — RAH v0.23.0 Post-Release State Cleanup

## Status

DOCUMENTATION-ONLY POST-RELEASE CLEANUP

This plan records the documentation-only descendant state after the verified
immutable publication of RAH v0.23.0. It does not redesign architecture,
change production Rust, frontend code, tests, Cargo versions or dependencies,
permissions, capabilities, profiles, workflows, ADRs, providers, the tag, or
the GitHub Release. It does not start v0.24 work.

## Starting release-preparation checkpoint

- Starting repository state is the clean Task 276 release-preparation source:
  `HEAD == master == origin/master ==
  05527ce10cc088bbaa09fc6792e0f26f6c85ac2b`.
- The direct parent is
  `533b0769618d25c1b9673a27c3e21af7a48809ca`.
- Task 276 exact-head CI is `34326184721` PASS.
- The immutable v0.23.0 release source forever remains
  `05527ce10cc088bbaa09fc6792e0f26f6c85ac2b`.

## Task 277 immutable publication identity

- Annotated tag: `v0.23.0`.
- Tag object: `5a27d84c269a0d57a8a6ad5f0fca89c06379e711`.
- Peeled source: `05527ce10cc088bbaa09fc6792e0f26f6c85ac2b`.
- Tag CI: `34330434792` PASS; branch `v0.23.0`, event `push`, completed,
  success, exact source SHA.
- GitHub Release: ID `385352229`, name `RAH v0.23.0`, tag `v0.23.0`.
- Published: `2026-09-09T08:43:12Z`.
- Release state: `draft=false`, `prerelease=false`, assets `0`.

Task 276 itself stopped before tag and Release creation. Publication completed
later in Task 277. Task 278 must not mutate any publication identity, publish
release assets, edit the GitHub Release, or move, recreate, delete, or force
update the tag.

## Prior immutable v0.22.0 identity

- Tag object: `eb91039521eac190efc85ef57ee2a5c28ca4e9fe`.
- Peeled target: `76895b4067c38167f3c41a3536f6616cffa8293a`.
- GitHub Release ID: `385176161`.

The prior release remains unchanged.

## Authorized documentation scope

The exact seven-file scope is:

1. `CHANGELOG.md`
2. `README.md`
3. `docs/ARCHITECTURE.md`
4. `docs/SECURITY.md`
5. `docs/RAH_V0.23_RELEASE_GATE.md`
6. `docs/plans/2026-09-09-v0.23-release-preparation.md`
7. `docs/plans/2026-09-09-v0.23-post-release-cleanup.md`

No Cargo/version/dependency change is authorized. No Rust, frontend, test,
ADR, permission, capability, profile, provider, or workflow file may change.
No new authority is added. No live rerun, model execution, release asset
publication, tag mutation, or Release mutation is part of this cleanup.

## Preserved v0.23 semantics and evidence

The released feature remains **HostExplicit Reviewed New-File Authoring**.
The human path remains:

```text
closed typed human {path, content}
 -> zero-effect Prepare
 -> complete bounded backend-derived review
 -> opaque single-use ticket
 -> ticket-only Confirm
 -> currentness/shared revalidation
 -> D2
 -> HostExplicit Started
 -> authorized_tool_dispatch
 -> ToolRegistry
 -> existing repo.create-file
 -> ADR 0013
```

The exact nine HostExplicit Tools remain `fs.read`, `repo.file-info`,
`repo.status`, `repo.diff`, `repo.diff-staged`, `repo.create-branch`,
`repo.patch`, `repo.edit-files`, and `repo.create-file`. The exact
`repo.create-file` statuses remain `ok`, `invalid_target`,
`precondition_failed`, `create_failed_known`, `write_failed_known`, and
`uncertain`.

ADR 0013 remains the underlying repository file-creation authority, ADR 0024
remains the reviewed HostExplicit new-file workflow, and ADR 0021 remains the
generic HostExplicit coordinator/currentness/D2 boundary. Stage and Commit
remain separate. The cleanup adds no authority and does not imply generic
filesystem write, arbitrary file creation, parent creation, delete/rename/
move, automatic Stage or Commit, HostExplicit `repo.commit`, model-selected or
provider-granted HostExplicit, crash atomicity, rollback, retry/replay, ticket
persistence/resume, race-free TOCTOU, OS sandboxing, network isolation, or
Linux/macOS production-live parity.

The accepted Windows evidence remains unchanged and is not rerun:

```text
Windows 10 Professional 10.0.19045 x64
Codex 0.149.0
codex.exe SHA-256:
14b7e6b2356e82d1d9275579eaa588757b4e0a501b65dcc19fccdf77bd83dc00

model:
gpt-5.6-terra

reasoning:
medium

Prepare:
0 Tool / 0 native attempts

Confirm:
1 Tool / 1 native attempt

generation tuple:
[1, 0, 0, 1]

model lifecycle:
0

MCP:
0

Process Plugin:
0

marker:
RAH_CREATE_FILE_HOSTEXPLICIT_LIVE_OK
```

This was human/host-initiated connected-current execution, not model-selected
authority. Task 278 does not rerun live certification or submit a model prompt.

## Validation and closure

Run the requested deterministic validation and documentation/scope checks:

```text
cargo fmt --check
cargo check --workspace
cargo test --workspace
cargo clippy --workspace --all-targets --all-features -- -D warnings
git diff --check
cargo metadata --no-deps --format-version 1
```

Metadata must report 13 workspace packages, all version `0.23.0`, all edition
2024. `git diff -- Cargo.toml Cargo.lock` must remain empty. The final diff
must contain exactly the seven paths above and no production, frontend, test,
ADR, permission, capability, profile, provider, or workflow change.

The preferred commit message is `docs: mark RAH v0.23.0 released`. Its direct
parent must be `05527ce10cc088bbaa09fc6792e0f26f6c85ac2b`. Before push,
`origin/master` must still equal that source. After push, exact-head CI must
be completed successfully for `master` at the final Task 278 SHA with event
`push`; queued or in-progress CI is not a pass.

The cleanup commit is a later documentation-only descendant and is not the
v0.23.0 release source. After exact-head CI, reverify the clean final head,
the unchanged v0.23.0 and v0.22.0 identities, and the clean worktree. Stop
after Task 278 exact-head CI and final verification. Do not start v0.24 work.

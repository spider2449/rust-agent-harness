# Task 177 — RAH v0.14.0 Release Preparation

## Scope

Prepare the completed bounded repository file rename/move milestone for the
`v0.14.0` release candidate. This task changes workspace release metadata and
release documentation only. It does not implement Rust functionality, change
authority semantics, add dependencies, create a tag, publish a GitHub Release,
or begin v0.15 work.

## Starting gate

- Authoritative starting HEAD:
  `ffe091530c5b29fbef579d1fdf8ada4986937d2b`.
- Starting `origin/master` matched that HEAD.
- Starting worktree was clean.
- Task 176 audit: PASS — ready for v0.14 release preparation.
- Task 176 exact-head CI: run `33726778388` — PASS.

## Changes

- Bump the workspace package version from `0.13.0` to `0.14.0`; retain edition
  `2024` and update only the corresponding workspace-local Cargo.lock records.
- Prepare the v0.14.0 changelog entry, README capability description,
  architecture/security wording, release gate, and this plan.
- Preserve ADR 0018's separate host-owned `repo.rename-file` authority,
  immediate pre-effect identity revalidation, one native no-replace effect,
  and no replay after possible effect.

## Evidence baseline

Task 175G is the certified Windows live evidence: PASS, closure commit
`2cd837ca39a653a86b5943b794aa3a5832116697`, exact-head CI `33725840696`, and
the certified `codex-cli 0.149.0` pair. The hashes are recorded in the release
gate. Windows is live-certified; Linux evidence is deterministic only and no
Linux live certification is claimed.

## Validation record

Actual results:

- `cargo fmt --check` — PASS.
- `cargo check --workspace` — PASS.
- `cargo test --workspace` — PASS.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` — PASS.
- `git diff --check` — PASS.
- `cargo metadata --no-deps --format-version 1` — PASS; exactly 12 packages,
  all `0.14.0`, edition `2024`.
- `node --check crates/rah-desktop/frontend/status.js` — PASS.
- `cargo build -p rah-desktop --release` — PASS.

Dependency comparison against the starting HEAD must report no dependency
drift. No Rust or frontend source changes are permitted.

## Release boundary

The candidate commit became the immutable `v0.14.0` release commit after Task
177 exact-head CI passed. Task 177 created no tag and published no GitHub
Release; those actions were deferred to Task 178.

## Final state

The prepared release candidate was subsequently released.

- Release commit:
  `52506521bdf838784dd45bb54df2d6bcff8bcd08`.
- Annotated tag: `v0.14.0`.
- Tag object: `9193423e96dd0cda2fd8f5ed5619ab2b58483acc`.
- GitHub Release:
  <https://github.com/spider2449/rust-agent-harness/releases/tag/v0.14.0>.
- Task 177: release preparation; exact-head CI run `33727731967` — PASS.
- Task 178: tagging and publication — COMPLETE.
- Task 179: post-release documentation/state cleanup.

The preparation chronology above is preserved. Task 177 did not itself create
the tag or publish the release.

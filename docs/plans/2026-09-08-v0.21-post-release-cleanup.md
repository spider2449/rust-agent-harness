# Task 255 — RAH v0.21.0 Post-Release State Cleanup

## Status

DOCUMENTATION UPDATED — AWAITING CLEANUP COMMIT / CI

This task is documentation-only and occurs after immutable Task 253
publication. It does not change the v0.21.0 release source or publication.

## Starting release identity

- Starting `HEAD == origin/master`: `7faa13a16425cc9d3bdb0dc540923eae8685ba90`.
- Starting worktree: clean.
- Release commit / peeled target: `7faa13a16425cc9d3bdb0dc540923eae8685ba90`.
- Annotated tag: `v0.21.0`.
- Tag object: `aa178f05d12d881745932800e8fc40a7431ce7b9`.
- Tag CI: `34199221802` PASS.
- Task 253 exact-head CI: `34198526278` PASS.
- Task 252 verdict: **A — MILESTONE COMPLETE — RELEASE PREPARATION MAY BEGIN**.
- Task 251 Windows live certification: PASS.

## Immutable publication verification

The required direct checks confirmed:

- `git cat-file -t v0.21.0`: `tag`.
- `git rev-parse "v0.21.0^{}"`:
  `7faa13a16425cc9d3bdb0dc540923eae8685ba90`.
- `git ls-remote --tags origin refs/tags/v0.21.0`:
  `aa178f05d12d881745932800e8fc40a7431ce7b9`.
- GitHub Release: `RAH v0.21.0`, ID `384519493`.
- GitHub Release: `draft=false`, `prerelease=false`.
- Published: `2026-09-08T07:27:35Z`.

The v0.20.0 release remains the prior immutable published release. No tag,
GitHub Release, or live gate is mutated or rerun by this task.

## Cleanup scope

Exactly these seven documentation files are in scope:

1. `CHANGELOG.md`
2. `README.md`
3. `docs/ARCHITECTURE.md`
4. `docs/SECURITY.md`
5. `docs/RAH_V0.21_RELEASE_GATE.md`
6. `docs/plans/2026-09-08-v0.21-release-preparation.md`
7. `docs/plans/2026-09-08-v0.21-post-release-cleanup.md`

No Rust, Cargo, dependency, frontend, test, ADR, permission, workflow, script,
or live-gate files are in scope.

## Documentation changes

- Current documentation identifies v0.21.0 as released and current.
- v0.20.0 is identified as the prior immutable published release.
- The release gate is recorded as **RELEASED — HISTORICAL RECORD**.
- The Task 253 publication record is appended without rewriting earlier
  pre-publication chronology.
- Task 251 Windows evidence and its limitations remain stated honestly.

## Release-source invariant

The v0.21.0 release source remains forever:

`7faa13a16425cc9d3bdb0dc540923eae8685ba90`

This later Task 255 documentation cleanup is not the v0.21.0 release source.
The annotated tag remains `aa178f05d12d881745932800e8fc40a7431ce7b9` and peels
to the release commit, never to the cleanup commit.

## Validation

Run sequentially:

- `cargo fmt --check`
- `cargo check --workspace`
- `cargo test --workspace`
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- `node --check crates/rah-desktop/frontend/status.js`
- `node crates/rah-desktop/frontend/status_authority_test.js`
- `cargo build -p rah-desktop --release`
- `git diff --check`
- `cargo metadata --no-deps --format-version 1`

Metadata must report 13 packages, all `0.21.0`, with edition `2024`.
`git diff -- Cargo.toml Cargo.lock` must be empty. No live gate rerun or model
prompt is part of this validation.

The final documentation audit must confirm that current release documents do
not say v0.21.0 is unreleased, while explicit historical pre-release
chronology may remain. It must also confirm the README current/prior status,
released Architecture/Security framing, release-gate history, unchanged tag
object and peeled target, and unchanged Release ID.

## Commit

Preferred commit message: `docs: mark RAH v0.21.0 released`.

Only the seven expected documentation files may be committed. The cleanup
commit is a descendant of the immutable release commit and is not the release
source.

## Exact-head CI

Before push, run `git fetch origin` and require
`origin/master == 7faa13a16425cc9d3bdb0dc540923eae8685ba90`. If the remote moved
unexpectedly, stop without a silent rebase.

After push, require cleanup-commit CI with `branch=master`, `event=push`,
`head_sha` equal to the cleanup commit, `status=completed`, and
`conclusion=success`. The run ID is recorded in the final completion report.

## Next task

Task 256 — RAH v0.22 Scope and Authority Roadmap. It is research-only, is not
implemented here, and is not started automatically.

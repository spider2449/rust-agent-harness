# Task 129 — RAH v0.10.0 release preparation

Date: 2026-08-29
Status: release preparation; no tag or GitHub Release.

## Starting checkpoints

- Audited starting SHA: `e57187afa831545888aff2418cb9e5d3668bab72`.
- Task 128 milestone audit: release-ready YES, subject to Task 129 gates.
- Task 128A CI recovery: final Unix mode-fixture repair at the starting SHA;
  exact-head CI `33244978381` PASS. The production `repo.patch` path did not
  change, and the original old refusal reason remains unrecoverable.

## Intended scope

Prepare only the immutable `0.10.0` release candidate: workspace/lockfile
version bump, changelog, release gate, narrow stale documentation repair,
deterministic validation, Desktop release build, release-preparation commit,
push, and exact-head CI. Task 129 does not create a tag, publish a release,
upgrade Codex, add dependencies, add an ADR, change authority, or implement a
product feature.

## Candidate changes

- Bump all 12 workspace packages from `0.9.0` to `0.10.0`.
- Describe actual v0.10 Desktop configuration, repository binding, and private
  SQLite persistence without widening claims.
- Add `docs/RAH_V0.10_RELEASE_GATE.md` with release status, dependency,
  SQLite/migration, authority, limitation, Task 120, and Task 128A evidence.

Changed files are limited to `Cargo.toml`, `Cargo.lock`, `CHANGELOG.md`, the
release gate, this plan, and narrowly necessary `README.md`,
`docs/ARCHITECTURE.md`, and `docs/SECURITY.md` updates.

## Deterministic and artifact gates

Run, before and after the release-preparation commit:

```text
cargo fmt --check
cargo check --workspace
cargo test --workspace -j 1
cargo clippy --workspace --all-targets --all-features -- -D warnings
git diff --check
cargo metadata --no-deps --format-version 1
node --check crates/rah-desktop/frontend/status.js
cargo build -p rah-desktop --release
```

`cargo metadata` verifies all packages are `0.10.0`; the release build records
the executable path, size, timestamp, and absence of adjacent `sqlite3.dll`.
If the known MSVC PDB artifact contention recurs, use a fresh isolated target
only and do not alter product code.

## Live evidence

Reuse milestone Windows PASS evidence for certified Codex connection, native Git
discovery, selected-repository observation/CWD binding, launch-CWD/AGENTS
isolation, inactive preferences, A/B transcript isolation, Resume/restart,
true V3-to-SQLite migration, and SQLite new-write persistence. Do not rerun
historical live infrastructure by default.

Task 120 remote successful generation remains **DEFERRED / NOT VALIDATED** and
transport confinement remains **NOT CLAIMED**. The release claim is only a
bounded host-selected initial endpoint under ADR 0015.

## Deferred immutable procedure

After exact-head CI is green, Task 130 may create the immutable annotated tag
and GitHub Release. This task stops before both actions.

# Task 211 — v0.17.0 Post-Release State Cleanup

## Result

Task 211 converts the v0.17.0 release-facing documentation from prepared
candidate state to completed historical release state. It is documentation-only
and does not alter the immutable release, Cargo metadata, dependencies, Rust
code, authority semantics, or live-provider evidence.

## Immutable release identity

The v0.17.0 tag remains permanently attached to
`dc9ae03598f1ac48a571bb118ae9fd971250a2b7`.

- Release commit: `dc9ae03598f1ac48a571bb118ae9fd971250a2b7`.
- Annotated tag: `v0.17.0`.
- Tag object: `8bf0bbeb14f3d7e42f1f53f2ee5c6098d561a4aa`.
- Peeled tag target: `dc9ae03598f1ac48a571bb118ae9fd971250a2b7`.
- GitHub Release:
  <https://github.com/spider2449/rust-agent-harness/releases/tag/v0.17.0>.
- GitHub Release ID: `383204639`.
- Release state: published, `draft=false`, `prerelease=false`.

Task 211 is a later docs-only master commit and must never become the v0.17.0
tag target.

## Publication evidence

- Task 209 exact-head CI: `33958718038` PASS.
- Task 210 tag-triggered CI: `33959105671` PASS.
- The release publication evidence above remains distinct from Task 211's
  post-release exact-head CI.

## Documentation cleanup

- Marked v0.17.0 released in `CHANGELOG.md`, with the release date and
  immutable publication metadata.
- Updated `README.md` from release-candidate wording to released wording while
  preserving the milestone description and certified Codex baseline.
- Converted `docs/RAH_V0.17_RELEASE_GATE.md` into a completed historical record.
- Updated the Task 209 preparation plan with final publication evidence.
- Updated stale v0.17 release-candidate status wording in
  `docs/ARCHITECTURE.md` and `docs/SECURITY.md` only; no architecture or
  security semantics changed.

## Task 207 limitation preserved

Task 207 remains **INCONCLUSIVE / externally blocked for the model-selected
external Tool execution sub-gate**. The documentation preserves that Windows
provider selection, provider admission, provider composition, effective
inventory, dynamic Tool advertisement, lifecycle ownership, and cleanup were
verified, while model-selected MCP/Process Plugin execution, live external
ToolStarted/ToolFinished lifecycle, live external-effect review invalidation,
live external-effect repository refresh, full Windows model-driven external
execution, and Linux external-provider live certification were not established.

The hardened certification failed closed at `ToolRequested=0`; no RAH product
defect was found; Codex baseline migration was not justified; and the
limitation was accepted as non-blocking by Tasks 207C and 208. No Task 207 PASS
or full-live external-provider claim is introduced.

## Validation

Task 211 validation results:

- `cargo fmt --check`: PASS.
- `cargo check --workspace`: PASS.
- `cargo test --workspace`: accepted with the known Windows fixture exception;
  `rah-desktop` reported 156 passed, 1 failed, and 2 ignored.
- Focused rerun of
  `tests::hardened_git_environment_requires_host_pinned_safe_directory_for_foreign_owner_diagnostic`:
  reproduced the same failure at `crates/rah-desktop/src/main.rs:6438` with
  `the diagnostic must reproduce Git's protected ownership refusal`.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: PASS.
- `git diff --check`: PASS.
- `cargo metadata --no-deps --format-version 1`: PASS; 13 packages, all
  `0.17.0`, edition `2024`.
- Frontend Node syntax checks and `status_authority_test.js`: PASS.
- Cargo manifests and `Cargo.lock`: unchanged.
- No new live external-provider Tool run was performed.

## Git state

The cleanup commit is docs-only, is newer than the immutable v0.17.0 release
commit, is pushed to `master`, and is verified against the exact-head CI result.
The worktree is clean and the v0.17.0 tag object and peeled target are
unchanged.

## Next milestone

Task 211 fully closes the v0.17 milestone. The next task is **Task 212 — RAH
v0.18 Scope and Authority Roadmap**, research-only first. Task 212 is not
started automatically.

# Task 165 — RAH v0.13.0 Release Preparation

## Starting gate

- Required starting `HEAD`: `0de2b2839fbd03d648ebbf2ba3f7d470d94ce794`.
- Starting exact-head CI: `33586765389` — PASS.
- Starting worktree: clean.
- Task 164 milestone audit: PASS.
- Prior v0.12 release preparation established one candidate commit containing
  the workspace version bump, workspace-local lockfile records, changelog,
  release gate, narrow stale-documentation corrections, local gates, push, and
  exact-head CI. No tag or publication belongs to preparation.

## Scope

Prepare the v0.13.0 release candidate for the completed bounded repository
file-deletion milestone. Change only release version state and release
documentation. Do not alter implementation, ADR 0017 semantics, dependency
versions, authority boundaries, tags, GitHub Releases, or unrelated technical
debt. Reuse Task 163 Windows live evidence; do not perform another destructive
live deletion.

## Candidate changes

- Bump the workspace package version from `0.12.0` to `0.13.0`, retaining Rust
  edition `2024`, and refresh only workspace-local `Cargo.lock` records.
- Add the v0.13.0 candidate changelog entry and release gate.
- Update README and architecture wording only where v0.13 makes the v0.12
  candidate wording stale.
- Preserve the separate ADR 0017 deletion authority, exact clean
  HEAD-tracked preimage requirement, no auto-stage boundary, no commit/ref/
  history expansion, Trusted Profile composition-only behavior, canonical
  public-name bridge discoverability, Desktop selected-repository binding,
  and Windows live evidence.

## Validation and release boundary

Run the full release gates, `cargo metadata --no-deps --format-version 1`, the
established Node and Codex baseline checks, and `cargo build -p rah-desktop
--release`. Verify all 12 workspace packages are `0.13.0`, no dependency drift
occurred, and the edition remains `2024`. Record actual results below before
the candidate commit.

The release gate must remain **RELEASE PREPARATION — NOT YET RELEASED** until
Task 166. It must not contain a v0.13 tag, GitHub Release, or publication
claim. Windows remains live-certified; Ubuntu/Linux evidence remains
deterministic only and Linux live certification is not established. Task 120
remains deferred and transport confinement remains not claimed.

## Completion record before commit

- Changed files: `Cargo.toml`, `Cargo.lock`, `CHANGELOG.md`, `README.md`,
  `docs/ARCHITECTURE.md`, `docs/SECURITY.md`, this plan, and
  `docs/RAH_V0.13_RELEASE_GATE.md`.
- Package version state: all 12 workspace packages are `0.13.0`; edition is
  `2024`; Cargo.lock has only the 12 corresponding workspace package-version
  changes; dependency delta is none.
- Release-gate status: release preparation, not yet released; tag, GitHub
  Release, and post-release cleanup remain not done.
- Preserved limitations: ADR 0017 boundaries, no auto-stage/commit/ref/history
  expansion, no rename/move or broader filesystem/Git/shell authority, Task
  120 deferred, transport confinement not claimed, process supervision is not
  OS sandboxing, and uncertain effects are not replayed or rolled back.
- Platform evidence wording: deterministic validation on Windows and
  Ubuntu/Linux where CI/tests provide evidence; Windows live-certified; Linux
  live certification not established; no macOS live claim.
- Local validation: `cargo fmt --check` PASS; `cargo check --workspace -j 1`
  PASS; `cargo test --workspace -j 1` PASS; `cargo clippy --workspace
  --all-targets --all-features -j 1 -- -D warnings` PASS; `git diff --check`
  PASS; `cargo metadata --no-deps --format-version 1` PASS; Node syntax check
  PASS; `cargo build -p rah-desktop --release` PASS.
- Baseline validation: the installed native Codex is `0.152.1`, while the
  certified `0.149.0` baseline is not installed as a native executable, so the
  established baseline test/verify could not run; no result is claimed.
- Release-preparation commit: pending.
- Exact-head CI: pending.
- Remaining blocker before completion: host availability of the exact
  certified baseline for the established baseline checks, unless the release
  process accepts the already recorded Task 163 certification and local
  baseline manifest as sufficient.

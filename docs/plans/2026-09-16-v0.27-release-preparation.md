# Task 336 — RAH v0.27.0 Milestone Audit and Release Preparation

Date: 2026-09-16

## Verdict

**READY FOR PUBLICATION — NOT YET RELEASED**

This accelerated Task 336 combines the final v0.27 milestone audit and release
preparation. It does not publish v0.27.0.

## Starting checkpoint

- Required checkpoint: `c2b3c070dbcc44ec877dc1f298657690d938fe98`.
- Pre-modification `HEAD`: `c2b3c070dbcc44ec877dc1f298657690d938fe98`.
- Pre-modification `origin/master`: `c2b3c070dbcc44ec877dc1f298657690d938fe98`.
- Pre-modification worktree: clean.
- Direct parent of the release-preparation commit: the required checkpoint
  above.

## Final v0.27 milestone audit

Tasks 328–335 are complete as one coherent chain:

```text
328 scope selected durable remembered workspace
  → 329 persistence/privacy authority research
  → 330 ADR 0028 and implementation contract
  → 331 / 331-A bounded persistence foundation and hardening
  → 332 inert startup integration
  → 333 catalog actions and explicit fresh re-admission
  → 334 Desktop UX and explicit privacy presentation
  → 335 security audit and Windows live certification
```

There is no unresolved mandatory blocker. No resolved design choice was
reopened. The release claim is **Durable Remembered Workspaces with Explicit
Fresh Re-Admission**: remembered candidates are descriptive across restart,
and executable authority requires explicit human fresh admission, current
facts, a fresh process-local `RepositoryMemberId`, and separate explicit
activation.

ADR 0027 remains authoritative for process-local membership, fresh identity and
currentness, one active repository, active-only composition, switching, and
repository-bound authority. ADR 0028 is additive and descriptive only.
Automatic re-admission, automatic activation, persisted authority restoration,
path-as-identity, union registries, and parallel active authority remain
forbidden. Catalog deletion is not member removal. Stage/Unstage and Commit
remain separate authorities.

## HostExplicit audit

The exact production HostExplicit set remains 11:

```text
fs.read
repo.file-info
repo.status
repo.diff
repo.diff-staged
repo.create-branch
repo.patch
repo.edit-files
repo.create-file
repo.delete-file
repo.rename-file
```

Still ineligible are `repo.create-directory`, `repo.commit`, MCP Tools,
Process Plugin Tools, fixtures/diagnostics, and unknown/provider-defined Tools.
No Task 336 authority, Tool, permission, frontend, Tauri, or ADR change is
made.

## Task 335 certification evidence

- Certified production source: `b90e63dd138732d616582d935b1fa45b842fcfe8`.
- Verdict: `PASS WITH EXPLICIT NONCLAIMS`.
- Marker: `RAH_V027_REMEMBERED_WORKSPACE_LIVE_OK`.
- Production exact-head CI: `35054734280` — PASS.
- Documentation head: `c2b3c070dbcc44ec877dc1f298657690d938fe98`.
- Documentation exact-head CI: `35055170395` — PASS.

The evidence includes P1/P2/P3 restart isolation, fresh member identity,
admission/activation separation, deletion isolation, current-facts rejection,
corrupt/future catalog behavior, privacy sentinel, zero startup candidate-path
access, A/B Git integrity, zero model requests and model Tool requests, zero
MCP and Process Plugin activations, zero automatic commits, zero network Git,
and bounded cleanup. It is host-driven production-backend evidence, not GUI
certification.

Required nonclaims are preserved:

```text
MODEL-SELECTED DYNAMIC TOOL DISPATCH NOT ESTABLISHED UNDER THE APPROVED GPT-5.6-TERRA LIVE GATE.
```

GUI automation, a real junction/reparse live fixture, true simultaneous
two-process live mutation, network isolation, race-free TOCTOU protection, and
automatic recovery/replay after uncertain native effects are not claimed.

## Release-preparation scope

- Bump the 13 workspace packages to `0.27.0`; keep Rust edition 2024.
- Update only internal RAH package-version records in Cargo.lock.
- Update current README, architecture, security, and changelog descriptions.
- Add the release gate and this combined Task 336 plan.
- Make no Rust, test, frontend, Tauri permission, or ADR changes.
- Leave `docs/RAH_V0.27_LIVE_CERTIFICATION.md` unchanged.

The persistence claims are the dedicated `RememberedWorkspaceStore`, closed
schema v1, `remembered-workspace.json`, opaque candidate IDs, bounds of 64
candidates / 256-byte labels / 64-byte IDs / 4096-byte location hints / 256 KiB
final catalog, same-directory atomic replacement, staged reread and exact
validation, Windows ancestor/final-file reparse hardening, persist-before-
publish, bounded coordination, passive startup loading, no startup probing,
path-free generic catalog presentation, explicit reveal, and fresh inert
admission. No encryption or race-free TOCTOU claim is made.

## Validation

The following gates are required before closure:

```text
cargo fmt --check
cargo check --workspace
cargo test --workspace -- --test-threads=1
cargo clippy --workspace --all-targets --all-features -- -D warnings
git diff --check
cargo metadata --no-deps --format-version 1
cargo build -p rah-desktop --release
node --check crates/rah-desktop/frontend/status.js
node crates/rah-desktop/frontend/remembered_workspace_test.js
node crates/rah-desktop/tauri_permission_test.js
```

Cargo metadata must report exactly 13 workspace packages, every package at
`0.27.0` and edition 2024, and no remaining workspace package at `0.26.0`.
The release build must compile as `rah-desktop v0.27.0`. Task 335 live
certification is carried forward because this task changes only version
metadata, Cargo.lock internal package records, and documentation.

The final validation outcomes, candidate SHA, clean parity, and exact-head CI
run are reported at Task 336 closure. The release-preparation commit does not
contain its own SHA; Task 337 may record that immutable source for publication.

### Completed local validation

- `cargo fmt --check` — PASS.
- `cargo check --workspace` — PASS.
- `cargo test --workspace -- --test-threads=1` — PASS; no failures. The
  Desktop suite reported 281 passed and 13 ignored; the ignored cases are
  explicitly host/live-gated tests.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` —
  PASS.
- `git diff --check` — PASS.
- `cargo metadata --no-deps --format-version 1` — PASS; exactly 13 workspace
  packages, all `0.27.0`, all edition 2024, and zero `0.26.0` workspace
  packages.
- `cargo build -p rah-desktop --release` — PASS; compiled as
  `rah-desktop v0.27.0`.
- `node --check crates/rah-desktop/frontend/status.js` — PASS.
- `node crates/rah-desktop/frontend/remembered_workspace_test.js` — PASS.
- `node crates/rah-desktop/frontend/status_authority_test.js` — PASS.
- `node crates/rah-desktop/frontend/repository_membership_test.js` — PASS.
- `node crates/rah-desktop/tauri_permission_test.js` — PASS.

Cargo.lock changed only the 13 internal RAH package-version records. No
external dependency version, checksum, source, feature, or graph drift was
observed. No live certification was rerun; Task 335 remains the certified
production behavior source.

## Publication stop

Task 336 must not create or push the `v0.27.0` tag, create a GitHub Release,
publish release notes, mark v0.27.0 released, claim a publication date, or
modify the immutable v0.26.0 release identity. The next task is Task 337 —
Publish RAH v0.27.0.

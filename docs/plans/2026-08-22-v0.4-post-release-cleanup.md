# v0.4.0 post-release documentation cleanup

Date: 2026-08-22
Status: Complete

## Confirmed release state

- Release version: `v0.4.0`.
- Release commit: `ebd6358`.
- Tag: `v0.4.0`, targeting `ebd6358`.
- CI passed and the GitHub Release is published and marked Latest.
- Codex baseline: `codex-cli 0.149.0`.
- The release baseline is Windows-verified; it does not claim cross-platform
  live verification.

## Stale text identified and corrected

- `CHANGELOG.md` described v0.4.0 as release preparation only. It now records
  the completed release, tag target, CI, and published GitHub Release.
- `CHANGELOG.md` described v0.3.0 as still being prepared. Git evidence shows
  `v0.3.0` targets `1968326`, so the entry now records that tag without making
  an unverified GitHub Release claim.
- `docs/RAH_V0.4_RELEASE_GATE.md` now distinguishes its historical local gate
  evidence from the subsequent tag, CI, and GitHub publication. Its checklist
  and final decision record the confirmed release state.
- `README.md`, `docs/ARCHITECTURE.md`, and `docs/SECURITY.md` contained no
  stale release-status wording and were left unchanged.

## Tag immutability

This cleanup is a post-release documentation commit. It must not move, delete,
recreate, or otherwise modify `v0.4.0`; the tag remains at `ebd6358`.

## Validation

```powershell
git diff --check
cargo check --workspace
git status --short
git diff --stat
git diff
git show v0.4.0 --no-patch --oneline
```

Validation confirms this task changes documentation only: no Rust source,
Cargo manifest, lockfile, dependency, public API, or ADR changes.

## Next development cycle

Recommended next task: **Task 046 — RAH v0.5 Scope and Authority Roadmap**.

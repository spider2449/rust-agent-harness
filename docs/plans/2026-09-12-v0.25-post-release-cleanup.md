# Task 307 — RAH v0.25.0 Post-Release State Cleanup

## Status and boundary

DOCUMENTATION-ONLY POST-RELEASE CLEANUP

RAH v0.25.0 is released. Task 307 converts release-facing documentation from
the Task 305 prepared state to the historical published state. It is a later
documentation-only descendant of the immutable release source, not the release
source itself.

No production code, API, authority, frontend, Tauri permission, provider,
Trusted Profile, ADR contract, CI workflow, Cargo version, dependency, tag, or
GitHub Release object is changed by this task. The destructive Windows rename
live test is not rerun.

## Immutable publication identity

- Immutable v0.25.0 release source:
  `a8b4d7b92f545a37d2ef2c8eae224f91c9d939c4`.
- Task 305 preparation CI: `34662233490` PASS.
- Task 306 performed publication with no repository commit.
- Annotated tag: `v0.25.0`.
- Tag object: `ea3c31aaf5190b632d7ef86387f7aff6004ae664`.
- Peeled target: `a8b4d7b92f545a37d2ef2c8eae224f91c9d939c4`.
- Tag message: `RAH v0.25.0`.
- Tagger timestamp: `2026-09-12T00:44:21Z`.
- Tag CI: `34662635116` PASS.
- GitHub Release ID/name: `387406579` / `RAH v0.25.0`.
- Published: `2026-09-12T00:47:29Z`.
- GitHub Release state: `draft=false`, `prerelease=false`, `assets=0`.

The immutable v0.25.0 release source forever remains
`a8b4d7b92f545a37d2ef2c8eae224f91c9d939c4`. The Task 307 descendant commit is
not the release source and must not move the tag or edit the GitHub Release.

## Prior immutable publication

- v0.24.0 annotated tag object:
  `5708cfd0078ba1593e86077bf88c0dddaa03c1bf`.
- v0.24.0 release source:
  `2d4cf0e4d89a461f54250ff81ad9808929d854f0`.
- v0.24.0 GitHub Release: `386128676` / `RAH v0.24.0`.
- v0.24.0 GitHub Release remains `draft=false`, `prerelease=false`,
  `assets=0`, published `2026-09-10T08:53:24Z`.

## Authorized exact scope

Modify exactly these seven documentation paths:

```text
CHANGELOG.md
README.md
docs/ARCHITECTURE.md
docs/SECURITY.md
docs/RAH_V0.25_RELEASE_GATE.md
docs/plans/2026-09-12-v0.25-release-preparation.md
docs/plans/2026-09-12-v0.25-post-release-cleanup.md
```

No `Cargo.toml` or `Cargo.lock` change is authorized. The workspace remains
13 packages, version `0.25.0`, edition `2024`, with no dependency, resolver,
member, provider protocol, Codex baseline, or Trusted Profile schema drift.

## Preserved capability and security contract

The release theme remains **HostExplicit Reviewed File Rename/Move
(`repo.rename-file`)**. ADR 0018 remains ordinary rename/move authority, ADR
0021 remains the generic HostExplicit coordinator/currentness/ticket/D2
boundary, and ADR 0026 remains the reviewed rename/move boundary. The exact 11
eligible HostExplicit Tools, provider/`repo.commit` ineligibility, Windows
case-equivalent collision correction, and all existing security nonclaims are
preserved.

The corrected Windows certification marker carried forward is
`RAH_REVIEWED_RENAME_HOSTEXPLICIT_LIVE_OK`. It is historical host-driven
connected-current evidence only; it was not rerun during publication or this
cleanup and does not claim model-selected reviewed rename certification.

## Validation and final closure

The required checks were run sequentially and passed:

```text
cargo fmt --check
cargo check --workspace
cargo test --workspace
cargo clippy --workspace --all-targets --all-features -- -D warnings
git diff --check
cargo metadata --no-deps --format-version 1
```

`cargo fmt --check`, `cargo check --workspace`, `cargo test --workspace`,
`cargo clippy --workspace --all-targets --all-features -- -D warnings`,
`git diff --check`, and metadata all passed. The metadata reports 13 packages,
13 workspace members, version `0.25.0`, and edition `2024`. The established
Desktop release build, frontend syntax/authority checks, and Tauri permission
checks also passed. No destructive Windows live test was rerun. The diff audit
contains exactly the seven paths above and no Cargo, Rust, JavaScript, HTML,
CSS, Tauri, ADR, CI, provider, or Profile change.

One focused commit is created with subject:

```text
docs: mark RAH v0.25.0 released
```

The final Task 307 cleanup commit SHA and direct parent are recorded in the
closure report. Exact-head cleanup CI is required to pass for that exact SHA
before closure; the run identity is recorded in the closure report. The
cleanup commit is a later descendant on `master` only and is not the v0.25.0
release source.

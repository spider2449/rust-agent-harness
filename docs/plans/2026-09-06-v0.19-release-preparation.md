# Task 231 — RAH v0.19.0 Release Preparation

## Status

IMPLEMENTED — AWAITING EXACT-HEAD CI

This plan records release preparation only. Task 231 is not complete until the
pushed preparation commit's exact-head CI completes successfully.

## Starting checkpoint

Repository identity was resolved dynamically. The intended remote is
`spider2449/rust-agent-harness`. Starting state:

- SHA: `757340061084b8fd6f591b9d985ada2c59b9113c`;
- origin/master: `757340061084b8fd6f591b9d985ada2c59b9113c`;
- commit: `docs: audit v0.19 local branch milestone`;
- worktree: clean;
- Task 230 exact-head CI: `34038812072` PASS;
- local and remote v0.19.0 tags: absent;
- GitHub Release `v0.19.0`: not found (404).

## Task 230 verdict

**VERDICT B — MILESTONE COMPLETE WITH DOCUMENTED LIMITATION — RELEASE
PREPARATION MAY BEGIN.**

The authority boundary, deterministic release gate, Desktop composition, and
Windows host-driven authority/effect path are complete. Model-selected branch
dispatch remains unestablished and nonblocking. The Task 230 plan remains
historical and is not rewritten.

## Scope

This task bumps the workspace to 0.19.0, prepares release documentation,
creates the v0.19 release gate, runs deterministic/release validation, and
commits/pushes this checkpoint. It does not change product Rust behavior,
authority, tests, frontend production files, ADR files, workflows, or live
experimental evidence. No tag or GitHub Release is created.

## Version preparation

Change only the workspace package version from 0.18.0 to 0.19.0. Preserve
edition 2024 and all 13 workspace members. Update Cargo.lock consistently and
verify that only the 13 workspace package version entries change; third-party
versions, checksums, and dependency edges remain unchanged.

## Product claims

v0.19 adds only ADR 0020 `repo.create-branch`: one absent ordinary local
`refs/heads/<validated-name>` at the host-captured attached committed HEAD,
using the fixed zero-old CAS and Git-owned fixed reflog. It does not switch,
checkout, create-and-switch, set tracking, mutate the index/worktree, or add
generic Git/ref/history authority.

## Authority / security boundary

The model input is name-only. The selected repository, Git executable,
captured OID, ref namespace, CAS old value, hooks, configuration, identity,
argv, environment, and output bounds are host-owned. Desktop registers the Tool
only when selected repository and valid `RepositoryBranchCreationAuthority`
are both present. Execute is only the outer gate; Trusted Profile/provider
metadata and frontend state cannot grant authority. Closed ASCII name and
collision validation, ordinary repository admission, identity/hook/config
hardening, one possible-effect attempt, six-result uncertainty taxonomy, and
no replay/rollback semantics are preserved.

## Windows host-driven live evidence

Task 229C certified the real Windows production path through Desktop authority,
Tool, registry dispatch, and native Git effect. It asserted exact captured-HEAD
targeting and unchanged protected/currentness state. The fresh successful branch
name and OID were internally asserted but not printed; this is a nonblocking
evidence-capture limitation and no effectful live rerun is permitted.

## Model-selected limitation

Task 229's two certified Codex 0.149.0 / `gpt-5.6-terra` attempts each recorded
`ToolRequested = 0`, `ToolStarted = 0`, and `ToolFinished = 0`. They did not
establish model-selected dispatch and are not described as branch execution
failures.

Windows host-driven Desktop repo.create-branch authority/effect path is certified. Model-selected repo.create-branch dispatch was not observed in two bounded Codex live attempts and is not certified.

## Prior preserved limitations

Task 207 remains INCONCLUSIVE / externally blocked for model-selected MCP /
Process Plugin Tool execution. Linux live branch/provider certification is not
claimed. There is no Trusted Profile/provider branch authority, generic or
remote Git, switching, rollback, OS sandboxing, network isolation, or race-free
external exclusion claim.

## Documentation changes

Prepare the v0.19 candidate framing and ADR 0020 summary in README.md,
CHANGELOG.md, docs/ARCHITECTURE.md, and docs/SECURITY.md. Add
docs/RAH_V0.19_RELEASE_GATE.md and this plan. Keep v0.18.0 as the current
immutable published release until separate publication, and retain the exact
required model-selection limitation wording.

## Workspace / dependency audit

Confirm 13 packages, all version 0.19.0, edition 2024, and no third-party
dependency drift. Inspect Cargo.toml/Cargo.lock and compare non-workspace
dependency name/version/checksum tuples. Confirm no changes under crates,
.github, or docs/adr.

## Validation

Sequential local validation completed with these actual results:

- baseline verification: PASS, certified `codex-cli 0.149.0`;
- `cargo fmt --check`: PASS;
- `cargo check --workspace`: PASS;
- focused branch gate: PASS, 29 passed;
- `cargo test -p rah-desktop`: PASS, 180 passed, 4 ignored;
- `cargo test --workspace`: PASS, all suites;
- clippy: PASS;
- frontend syntax: PASS;
- frontend authority static test: PASS;
- Desktop release build: PASS;
- `git diff --check`: PASS;
- metadata: PASS, 13 packages, all `0.19.0`, edition 2024.

Documentation, dependency, and scope audits found no drift. The exact-head
pre-commit scope audit passed: only the eight expected files are staged, with
no `crates`, `.github`, or `docs/adr` diff.

Do not rerun `windows_live_desktop_repo_create_branch`,
`windows_live_desktop_repo_create_branch_host_driven`, or any Task 229 model
prompt.

## Commit

After all local gates pass, commit exactly the release-preparation files with:

`docs: prepare RAH v0.19.0 release`

Before push, fetch origin and require origin/master remains at
`757340061084b8fd6f591b9d985ada2c59b9113c`. Do not force-push or rebase.

## Exact-head CI

Push master only after the local gates pass. Record the full preparation commit
SHA, verify `HEAD == origin/master` and a clean worktree, then wait for CI whose
`head_sha` is exactly that commit. Task 231 becomes complete only when that CI
is completed successfully. No tag may be created before that PASS.

## Publication boundary

This task creates no v0.19.0 tag and publishes no GitHub Release. Publication,
annotated tagging, tag-triggered CI, and immutable release verification belong
to Task 232.

## Next task

After Task 231 is complete, the next task is Task 232 — Publish RAH v0.19.0.
Task 232 is not started automatically.

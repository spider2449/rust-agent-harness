# Task 167 — RAH v0.13.0 Post-Release State Cleanup

## Starting state

- Starting `HEAD` and `origin/master`:
  `a432d7ecc4a5a564288e2bd50b550055b94920cf`.
- Starting worktree: clean.
- Released version: `RAH v0.13.0`.
- Immutable release commit:
  `a432d7ecc4a5a564288e2bd50b550055b94920cf`.
- Annotated tag: `v0.13.0`.
- Tag object: `4a40d8fd0f6065c771dd1a78e4808df0cd02c8e7`.
- Peeled tag target:
  `a432d7ecc4a5a564288e2bd50b550055b94920cf`.
- Exact-head release CI: `33588660637`, PASS.
- GitHub Release: published, non-draft, and non-prerelease.

## Scope and completed state

This task records the post-release historical state in the changelog, release
gate, and release-preparation plan. The v0.13.0 release publication succeeded,
the source worktree remained clean after publication, and no tag movement
occurred.

The cleanup contains no Rust, Cargo, dependency, package-version, or product
change. It does not modify the published GitHub Release or start v0.14 work.

Task 163 Windows live deletion validation remains the certified live evidence.
The certified Codex runtime for v0.13 is exactly `codex-cli 0.149.0`.
Locally installed `0.152.1` is not retroactively certified.

Deterministic validation evidence is established on Windows and Ubuntu/Linux
where CI/tests provide it. Windows is live-certified; Linux live certification
is not established.

The Task 163 observability item remains non-blocking historical technical debt:
`tool_finished.result` was `null` because the evidence helper did not capture
`ToolContent::Json`. The initial Desktop tool discoverability failure and the
CRLF/raw-byte `precondition_failed` attempt remain unchanged historical
evidence.

Task 120 remains deferred where documented. Transport confinement and network
isolation are not claimed; process supervision is not OS sandboxing; rollback
is not guaranteed; and uncertain external effects are not replayed.

## Validation and completion

- `git diff --check`: PASS.
- Changes are documentation-only: no Rust source, `Cargo.toml`, `Cargo.lock`,
  dependency, or version changes.
- The immutable `v0.13.0` tag was not modified, recreated, deleted, or moved.
- One coherent post-release cleanup commit is created and pushed as a
  descendant of the release commit. Its exact-head CI must pass before Task
  167 is complete.

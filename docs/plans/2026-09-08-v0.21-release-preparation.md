# Task 253 — RAH v0.21.0 Release Preparation

## Status

RELEASE PREPARATION — NOT TAGGED OR PUBLISHED

Task 253 is complete only after the preparation commit is pushed and its
push-triggered exact-head CI completes successfully. Task 254 publication is
not started automatically.

## Checkpoint

- Repository root resolved dynamically with `git rev-parse --show-toplevel`.
- Required `HEAD == master == origin/master ==
  563301f2627b50c5c256ad221c34a931b28aa1e2`.
- Task 252 exact-head CI: `34196931238` PASS.
- Starting worktree: clean.
- v0.21.0 tag: absent.
- GitHub Release v0.21.0: absent.

## Audit verdict and release scope

Task 252 Verdict A is carried forward: **MILESTONE COMPLETE — RELEASE
PREPARATION MAY BEGIN**.

The v0.21 milestone is HostExplicit Reviewed Single-File Patch Authoring. Its
product flow is:

```text
inspect -> typed human repo.patch Prepare -> exact bounded review
 -> ticket-only Confirm -> existing repo.patch -> inspect diff
 -> existing Stage / Unstage -> existing reviewed Commit
```

ADR 0012 remains the sole underlying worktree-content mutation authority. ADR
0021 remains the general HostExplicit dispatch/currentness/D2 boundary. ADR
0022 adds only the reviewed H1 workflow and no generic write authority.

The exact seven eligible Tools are `fs.read`, `repo.file-info`, `repo.status`,
`repo.diff`, `repo.diff-staged`, `repo.create-branch`, and `repo.patch`.
`repo.create-file`, `repo.edit-files`, `repo.delete-file`, `repo.rename-file`,
`repo.create-directory`, `repo.commit`, MCP Tools, and Process Plugin Tools are
not eligible.

## Version bump

- Workspace version: `0.20.0` -> `0.21.0`.
- All 13 workspace packages resolve to `0.21.0`.
- Edition remains `2024`.
- Cargo.lock changes are limited to internal RAH workspace package versions.
- No dependency version, source, checksum, edge, addition, or removal drift.

## Documentation scope

The exact changed-file scope is:

- `Cargo.toml`
- `Cargo.lock`
- `CHANGELOG.md`
- `README.md`
- `docs/ARCHITECTURE.md`
- `docs/SECURITY.md`
- `docs/RAH_V0.21_RELEASE_GATE.md`
- `docs/plans/2026-09-08-v0.21-release-preparation.md`

No accepted ADR is revised. No Rust, frontend production, test, script,
workflow, dependency, or live-gate implementation is changed.

## Security and evidence carried forward

The H1 request contains only typed `path`, `expectedOldText`, and
`replacementText`. The host derives SHA-256/length and canonical ToolInput.
Shared `RepositoryPatchPreparer` is non-effectful. Exact R4 review has complete
escaped mutation-relevant material with no hidden review content. The opaque
process-local single-use five-minute ticket carries the preparation binding;
Confirm receives only its ID.

Confirm revalidates the preparation before D2 preflight and `Started`, then
uses `authorized_tool_dispatch` after `Started`; existing `repo.patch` performs
the mutation. Strict result classification and conservative post-start
malformed/uncertain handling remain in force. There is no retry, replay,
rollback, restore-preimage, or compensation. Patch source text is absent from
generic activity/persistence. Effectful start invalidates old reviewed-commit
authorization; refresh may create a new `ReadyToAuthorize` review without
automatically authorizing it. Content mutation alone does not increment
`repository_generation`.

Task 251 Windows live evidence is reused without rerun:

- final live docs commit: `957db5d7dce15b93e5a081862fbfd0ae086ef539`;
- final CI: `34195501235` PASS;
- production coordinator fix: `36d4cfa3e11a7c5ad0a37d65112b684ebfbbfe0c`;
- Prepare: 0 Tool executions / 0 native replacements;
- Confirm: 1 Tool execution / 1 native replacement;
- result `ChangedVerified`, exact expected postimage, protected Git state
  unchanged, fresh review semantics correct, model lifecycle `0/0/0`, MCP 0,
  Process Plugin 0.

The Task 251 chronology remains: Attempt 1 exposed the pre-effect coordinator
defect; Task 251A fixed async Prepare reservation/finalization; Attempt 2
mutated successfully but hit an over-strict assertion with no retry/rollback
and preserved its fixture; Task 251C corrected only that assertion; Attempt 3
was a fresh disposable-repository live PASS.

## Validation

Run sequentially, without ignored live tests or a model prompt:

```text
cargo fmt --check
cargo check --workspace
cargo test --workspace
cargo clippy --workspace --all-targets --all-features -- -D warnings
git diff --check
cargo metadata --no-deps --format-version 1
node --check crates/rah-desktop/frontend/status.js
node crates/rah-desktop/frontend/status_authority_test.js
cargo build -p rah-desktop --release
```

Metadata must report 13 packages, all version `0.21.0`, all edition `2024`.
Cargo.toml/Cargo.lock inspection must show only internal workspace version
bookkeeping and no dependency drift.

## Commit and exact-head CI

Before commit/push, run `git fetch origin` and require
`origin/master == 563301f2627b50c5c256ad221c34a931b28aa1e2`; a moved baseline
requires stopping without a silent rebase.

Commit all release-preparation changes with:

`docs: prepare RAH v0.21.0 release`

The final preparation commit SHA is recorded after the commit exists. Push
`master` normally. Require push event CI for branch `master` whose head SHA is
exactly that commit, with status `completed` and conclusion `success`. The
exact-head CI run is recorded after it exists.

## Publication boundary and next task

Task 253 creates no `v0.21.0` tag, GitHub Release, artifact publication, or
live rerun. Task 254 — Publish RAH v0.21.0 — is next and is not started
automatically.

## Subsequent publication record

Task 253 was subsequently published as the immutable RAH v0.21.0 release:

- Task 253 release commit: `7faa13a16425cc9d3bdb0dc540923eae8685ba90`.
- Task 253 exact-head CI: `34198526278` PASS.
- Annotated tag: `v0.21.0`.
- Tag object: `aa178f05d12d881745932800e8fc40a7431ce7b9`.
- Peeled target: `7faa13a16425cc9d3bdb0dc540923eae8685ba90`.
- Tag CI: `34199221802` PASS.
- GitHub Release ID: `384519493`.
- Published: `2026-09-08T07:27:35Z`.
- Immutable release status: released and current.

Task 255 is a subsequent documentation-only cleanup after publication. It
does not change the v0.21.0 release identity or source.

# Task 327 — RAH v0.26.0 Post-Release State Cleanup

Date: 2026-09-15
Task type: documentation-only post-release cleanup
Status: **COMPLETED — VALIDATED DOCUMENTATION CLEANUP**

## Scope and immutable checkpoint

RAH v0.26.0 is released. Task 327 converts release-facing documentation from
the Task 325 preparation state to the factual historical state `RELEASED`.
The starting `master` and immutable release source are:

`8b22b18739a54b752ce11e791fe380e522c10049`

The immutable v0.26 publication identity is:

- annotated tag: `v0.26.0`;
- tag object: `f60ee2465c1819b59b5671f20387b74734ace019`;
- peeled target: `8b22b18739a54b752ce11e791fe380e522c10049`;
- tag message: `RAH v0.26.0`;
- release-preparation CI: `34965819523` — PASS;
- tag CI: `34966821012` — PASS;
- GitHub Release ID: `389111343`, name `RAH v0.26.0`;
- published: `2026-09-15T12:07:53Z`;
- `draft=false`, `prerelease=false`, assets: 0.

Task 325 prepared the immutable source and stopped before publication. Task
326 later created the annotated tag and GitHub Release without a repository
commit. Task 327 is a later documentation-only descendant and is explicitly
not the v0.26.0 release source.

## Exact cleanup scope

The exact seven changed documentation files are:

```text
CHANGELOG.md
README.md
docs/ARCHITECTURE.md
docs/SECURITY.md
docs/RAH_V0.26_RELEASE_GATE.md
docs/plans/2026-09-15-v0.26-release-preparation.md
docs/plans/2026-09-15-v0.26-post-release-cleanup.md
```

No Cargo.toml, Cargo.lock, Rust source, tests, frontend source, Tauri
permissions, dependencies, workspace version, ADR, provider schema, Tool
schema, release tag, or GitHub Release metadata changes are in scope.

## Preserved product and evidence boundaries

The release theme remains **Explicit Multi-Repository Membership and Active-Only
Repository Switching**: multiple repositories may be explicitly admitted per
process, exactly one repository is active, switching is explicit, active-only
Tool composition is host-owned, stale state is invalidated, and membership is
process-local and nonpersistent.

Task 324-C-R4 remains host-driven PASS with marker
`RAH_V026_HOST_DRIVEN_MULTI_REPOSITORY_LIVE_OK`. The exact HostExplicit set
remains 11:

```text
fs.read
repo.file-info
repo.status
repo.diff
repo.diff-staged
repo.create-branch
repo.patch
repo.edit-files
repo.create-file
repo.delete-file
repo.rename-file
```

`repo.create-directory`, `repo.commit`, MCP Tools, Process Plugin Tools,
fixtures/diagnostics, and unknown/provider-defined Tools remain ineligible.
Stage/Unstage remain separate host index authority and Commit remains separate
repository-bound reviewed authority.

`MODEL-SELECTED DYNAMIC TOOL DISPATCH NOT ESTABLISHED UNDER THE APPROVED GPT-5.6-TERRA LIVE GATE.`
The host-owned Windows authority/effect certification passed; current
model-selected dynamic Tool behavior was not established. This is not a
product failure claim.

## Validation and closure record

The required documentation-cleanup validation is:

```text
cargo fmt --check
cargo check --workspace
cargo test --workspace
cargo clippy --workspace --all-targets --all-features -- -D warnings
git diff --check
cargo metadata --no-deps --format-version 1
cargo build -p rah-desktop --release
```

Validation passed: `cargo fmt --check`, `cargo check --workspace`,
`cargo test --workspace`, `cargo clippy --workspace --all-targets
--all-features -- -D warnings`, `git diff --check`, `cargo metadata
--no-deps --format-version 1`, and `cargo build -p rah-desktop --release`.
All executed tests passed; only explicitly gated live/host probes remained
ignored. Metadata verified 13 packages, all `0.26.0`, Rust edition 2024, no
dependency drift, and no Cargo.lock drift. No external live certification was
rerun because source/runtime bytes are unchanged.

Task 327 cleanup SHA: to be recorded in the final closure report after the
cleanup commit. It is a later documentation-only descendant and is not the
v0.26.0 release source.

Before and after cleanup, preserve v0.26 tag object
`f60ee2465c1819b59b5671f20387b74734ace019`, peeled target
`8b22b18739a54b752ce11e791fe380e522c10049`, and GitHub Release `389111343`
with unchanged tag, name, publication timestamp, `draft=false`, and
`prerelease=false`. Preserve v0.25 tag `v0.25.0`, tag object
`ea3c31aaf5190b632d7ef86387f7aff6004ae664`, peeled source
`a8b4d7b92f545a37d2ef2c8eae224f91c9d939c4`, and GitHub Release `387406579`.

After commit and push, record exact-head Task 327 CI, `HEAD == origin/master`,
and a clean worktree. The next project state is fully released v0.26.0; do not
start v0.27 implementation automatically. The next task is a fresh v0.27
scope/authority roadmap or another explicitly chosen direction.

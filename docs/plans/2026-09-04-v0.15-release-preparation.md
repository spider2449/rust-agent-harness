# Task 188 — RAH v0.15.0 Release Preparation

## Scope

Prepare the v0.15.0 release candidate through workspace version metadata and
release documentation only. No Rust or frontend implementation changes, new
authority, dependency additions, ADR changes, tag, or GitHub Release are part
of Task 188.

## Authoritative starting state

- Starting HEAD / `TASK187_FULL_SHA`:
  `5e6ebd8b84d61b5018388c06df908553c5d77f33`.
- `origin/master` matched HEAD and the starting worktree was clean.
- Task 187 audit: PASS — ready for v0.15 release preparation.
- Task 187 exact-head CI: run `33828160578` — PASS.

## Preparation changes

- Workspace package version: `0.14.0` -> `0.15.0`; edition remains `2024`.
- Cargo.lock: only naturally required workspace-local package versions.
- Documentation: CHANGELOG, README, architecture/security models, release
  gate, and this plan document the bounded `repo.create-directory` milestone.
- Expected changed files are limited to release metadata and documentation;
  no Rust, frontend, ADR 0019, or external dependency files are changed.

## v0.15 evidence preserved

Task 186 remains the authoritative Windows live certification:

- commit `86c3eb38ddb3bf378c0001a11e594aba7cdea22d`;
- CI run `33827552359` — PASS;
- evidence `docs/plans/2026-09-04-windows-live-repo-create-directory-validation.md`;
- certified `codex-cli 0.149.0` pair:
  `codex.exe` SHA-256
  `14b7e6b2356e82d1d9275579eaa588757b4e0a501b65dcc19fccdf77bd83dc00` and
  `codex-code-mode-host.exe` SHA-256
  `3c6726ab12b8de7c0bccecf4551af686d9dbe1b9fcdaee90bd66f60837943ac2`.

Windows is live-certified. Ubuntu/Linux is deterministic-validation evidence
only; Linux live certification is not established. The Task 186 fixture and
history are not rerun or rewritten.

## Validation record

Record the exact Task 188 commands and results here after execution:

- `cargo fmt --check` — PASS.
- `cargo check --workspace` — PASS.
- `cargo test --workspace` — PASS.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` — PASS.
- `git diff --check` — PASS.
- `cargo metadata --no-deps --format-version 1` — PASS; 12 packages, all
  `0.15.0`, edition `2024`.
- `node --check crates/rah-desktop/frontend/status.js` — PASS.
- `cargo build -p rah-desktop --release` — PASS.
- Dependency comparison against starting HEAD — no external dependency drift.

## Release boundary

The single Task 188 commit is the proposed immutable v0.15.0 release commit.
Its identity is intentionally `TBD` in the pre-commit release gate according
to repository precedent and is reported after commit creation as the actual
full SHA. It must be pushed and pass exact-head CI before Task 188 is complete.

- No v0.15.0 tag created.
- No GitHub Release published.
- v0.15.0 is not yet released.
- Next task, only after exact-head CI PASS: Task 189 release publication.

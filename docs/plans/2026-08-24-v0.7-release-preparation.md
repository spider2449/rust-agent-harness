# Task 080 — RAH v0.7 Release Preparation

Date: 2026-08-24

## Starting point

- Task 079 RELEASE READY commit:
  `8e06686f73c7cda13fb816448d3d5096aeccf116`.
- Task 079 CI: `32699236755` (`completed / success`).
- Certified live baseline: `codex-cli 0.149.0`, SHA-256
  `14b7e6b2356e82d1d9275579eaa588757b4e0a501b65dcc19fccdf77bd83dc00`.

## Release-preparation changes

- Bump all 11 workspace packages from `0.6.0` to `0.7.0` without dependency
  upgrades.
- Update the current README release references, CHANGELOG, and v0.7 release
  gate while preserving the corrected bounded `repo.patch` wording.
- Preserve profile version 1, canonical `repo.patch`, the trusted-profile
  binding format, all authority boundaries, and the Codex 0.149.0 baseline.

## Interruption and resumption

- The initial Task 080 release-preparation live gate failed because local Codex
  configuration drift made the result non-reproducible.
- Task 080A diagnosed that drift. Task 080B added an isolated certified
  `CODEX_HOME` with `gpt-5.4` and `medium` reasoning. Task 080C changed the
  live examples so host structural assertions, rather than assistant prose,
  emit the success markers.
- Task 080R resumes this release preparation with that corrected gate. These
  changes do not alter `repo.patch`, Generic Tool Bridge production behavior,
  or RAH authority.

## Required evidence

- Focused and full deterministic Cargo format, check, test, clippy, diff, and
  metadata gates.
- Baseline script regression plus certified archive version and SHA-256
  verification.
- Windows native certified live adapter smoke marker:
  `RAH_ECHO_BRIDGE_OK`, emitted by the host after structural assertions.
- One fresh certified observer-only run requiring host marker
  `RAH_REPOSITORY_OBSERVERS_LIVE_OK`.
- Three fresh certified Windows native multi-patch runs, each requiring:
  `RAH_MULTI_PATCH_LIVE_OK`.
- Exact-head Ubuntu CI after the release-preparation commit is pushed.

## Platform scope

The release may claim Windows x64 certified-baseline management and Windows
native Codex live validation at 0.149.0, plus Ubuntu deterministic CI. It does
not claim Linux/macOS live Codex validation, Windows ARM64 baseline management,
or universal binary portability.

## Completion record

The release-preparation commit SHA and exact-head CI result are recorded in the
Task 080 completion report after the commit is created and pushed. No tag or
GitHub Release is created by this task.

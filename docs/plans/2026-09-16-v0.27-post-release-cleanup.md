# Task 338 — RAH v0.27.0 Post-Release State Cleanup

Date: 2026-09-16

## Purpose and result

Task 337 successfully published RAH v0.27.0. Task 338 converts the prepared
documentation into an accurate historical released record. This plan and its
commit are later documentation-only descendants; they are not the immutable
v0.27.0 release source.

## Immutable publication identity

- Release source: `1297787df07c725e41deb758cc7ecab5593b28d8`.
- Annotated tag: `v0.27.0`.
- Tag object: `587bb55747ac1c9cdf1c02bc56e7dff244ede93b`.
- Peeled target: `1297787df07c725e41deb758cc7ecab5593b28d8`.
- Release-preparation CI: `35061634078` — PASS.
- Tag CI: `35063921592` — PASS.
- GitHub Release ID: `389683736`.
- Release name: `RAH v0.27.0`.
- Published: `2026-09-16T06:31:29Z` (`draft=false`, `prerelease=false`).
- Assets: 0.

The tag remains annotated and continues to peel to the immutable release
source above. Task 338 cleanup HEAD must differ from that source.

## Task 338 scope

Changed files are documentation only:

- `CHANGELOG.md`;
- `docs/RAH_V0.27_RELEASE_GATE.md`;
- `docs/plans/2026-09-16-v0.27-release-preparation.md`; and
- this cleanup record.

There are no Rust, Cargo, Cargo.lock, dependency, public API, frontend,
permission, runtime, production behavior, authority, ADR, tag, or GitHub
Release changes. ADR 0027 remains unchanged and ADR 0028 remains additive.

## Preserved v0.27 historical claims

The release theme remains **Durable Remembered Workspaces with Explicit Fresh
Re-Admission**. Remembered candidates persist descriptively; restart restores
zero executable repository authority; fresh explicit admission is required; a
new process-local `RepositoryMemberId` is allocated; activation is separately
explicit; the generic catalog path is path-free; full location reveal is
explicit; and catalog deletion is not member removal.

The exact production HostExplicit set remains 11. Stage/Unstage remain separate
host index authority, Commit remains separate reviewed authority, Codex's
certified baseline remains `0.149.0`, and ADR 0027/0028 retain their existing
boundaries. The workspace remains 13 packages at `0.27.0`, all on edition 2024.

Task 335 remains recorded exactly as certified:

- production source: `b90e63dd138732d616582d935b1fa45b842fcfe8`;
- verdict: `PASS WITH EXPLICIT NONCLAIMS`;
- marker: `RAH_V027_REMEMBERED_WORKSPACE_LIVE_OK`.

The required nonclaim remains verbatim:

`MODEL-SELECTED DYNAMIC TOOL DISPATCH NOT ESTABLISHED UNDER THE APPROVED GPT-5.6-TERRA LIVE GATE.`

GUI automation was not certified; a real junction/reparse live fixture was not
executed; true simultaneous two-process live mutation was not executed; no
network isolation, race-free TOCTOU guarantee, or automatic recovery/replay
guarantee after uncertain native effects is claimed.

## Final verification

Task 338 validation is focused documentation-only validation:

```text
git diff --check
cargo check --workspace
git rev-parse "v0.27.0^{}"
git cat-file -t v0.27.0
git rev-parse v0.27.0
gh release view v0.27.0 --json databaseId,name,tagName,publishedAt,isDraft,isPrerelease,assets
git status --short
git diff --stat
```

The release identity checks must report type `tag`, tag object
`587bb55747ac1c9cdf1c02bc56e7dff244ede93b`, and peeled target
`1297787df07c725e41deb758cc7ecab5593b28d8`. The GitHub Release must remain ID
`389683736`, tag `v0.27.0`, `draft=false`, `prerelease=false`, and assets 0.

The final Task 338 commit SHA, direct parent
`1297787df07c725e41deb758cc7ecab5593b28d8`, exact-head Task 338 CI result,
`HEAD == origin/master`, and clean-worktree result are reported at closure.
The cleanup commit is explicitly not the v0.27.0 release source.

## Closure

After Task 338 passes, v0.27.0 is fully closed. The next development task
starts a new milestone or scope decision rather than extending v0.27 after
release.

# Task 153 - v0.12 release preparation

## Starting checkpoint

- Starting SHA: `62d4adba6fe2c7cb1893b405b6964c38bb95352c`.
- Starting `origin/master`: `62d4adba6fe2c7cb1893b405b6964c38bb95352c`.
- Starting commit: `docs: audit v0.12 Desktop authoring milestone`.
- Task 152 exact-head CI: `33477249271`, PASS.
- Task 152 audit decision: **READY FOR v0.12 RELEASE PREPARATION**.
- Starting worktree: clean; `.vscode/` remains untouched.
- Released baseline: RAH v0.11.0.

## Intended scope

Prepare exactly one immutable RAH v0.12.0 release candidate for the audited
Windows Desktop bounded repository-authoring workflow. This task permits only
the version bump, workspace-local lockfile records, candidate changelog,
release gate, narrowly necessary current-release documentation corrections,
release build, deterministic validation, commit, push, and exact-head CI.

No Rust or JavaScript capability, authority, dependency, ADR, Codex baseline,
tag, GitHub Release, post-release cleanup, or `.vscode/` change is permitted.

## Candidate work

- Change the workspace package version from `0.11.0` to `0.12.0`, retaining
  edition `2024`, and verify all 12 resolved workspace packages inherit it.
- Refresh only the corresponding workspace-local package version records in
  `Cargo.lock`.
- Add the v0.12.0 changelog candidate without claiming a tag, release commit,
  tag CI, or GitHub Release.
- Create the v0.12 release gate with pending release-commit and exact-head-CI
  fields; it remains release preparation, not historical release evidence.
- Carry forward the audited Task 151 Windows evidence, including its explicit
  observability limitations and the manifest-v2 complete Codex pair.

## Validation and release build

Run serially:

- `cargo fmt --check`
- `cargo check --workspace -j 1`
- `cargo test --workspace -j 1`
- `cargo clippy --workspace --all-targets --all-features -j 1 -- -D warnings`
- `git diff --check`
- `cargo metadata --no-deps --format-version 1`
- `node --check crates/rah-desktop/frontend/status.js`
- `cargo build -p rah-desktop --release`
- `scripts/test-codex-baseline.ps1`
- `scripts/codex-baseline.ps1 verify 0.149.0`

The Desktop release build records the canonical executable path, size,
last-write timestamp, SHA-256, and adjacent `sqlite3.dll` state. The certified
Codex bundle is external host-managed runtime state and is not bundled into the
Desktop executable.

## Dependency and authority audit

Compare the candidate manifest and lockfile changes against the starting
checkpoint. Required conclusion: **DEPENDENCY DELTA = NONE**; only local
workspace package-version records may change in `Cargo.lock`.

ADRs 0010 through 0016 remain authoritative. Task 146 remains **SKIPPED BY
DESIGN**. Task 120 remains **DEFERRED / NOT VALIDATED** and transport
confinement remains **NOT CLAIMED**. The candidate introduces no generic Git,
shell/process, filesystem-write, branch/ref, network, credential, or rollback
authority.

## Immutable release procedure

After all local gates pass, create one commit with message
`docs: prepare RAH v0.12.0 release`, push normally, verify local `HEAD` equals
`origin/master`, and require a new successful exact-head CI run. That commit is
then the proposed immutable v0.12.0 tag target.

Task 153 must not create `v0.12.0`, publish a GitHub Release, or alter the
committed candidate gate to insert same-commit identities. Task 154 is the
separate tag and publication task after the exact-head CI passes.
